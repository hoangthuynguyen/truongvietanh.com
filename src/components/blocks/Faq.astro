---
import type { BlockFaq } from '../../types/cms';
interface Props { data: BlockFaq; context: string; }
const { data } = Astro.props;
---
<section class="b-faq">
  <div class="b-container b-faq-inner">
    {data.eyebrow && <p class="b-eyebrow">{data.eyebrow}</p>}
    {data.title && <h2 class="b-title">{data.title}</h2>}
    <div class="b-faq-list">
      {data.items.map((item, i) => (
        <details class="b-faq-item" open={i === 0}>
          <summary>{item.question}</summary>
          <div class="b-faq-answer" set:html={item.answer} />
        </details>
      ))}
    </div>
  </div>
</section>
