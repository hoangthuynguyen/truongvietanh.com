---
import BaseLayout from '../layouts/BaseLayout.astro';
import { schoolFacts } from '../data/school-facts';
import { giaoVienChuNhiem, giaoVienBoMon, hienThi } from '../data/doi-ngu-giao-vien';
import HeaderBase from '../components/HeaderBase.astro';
import FooterBase from '../components/FooterBase.astro';

const tabs = [
  { id: 'chu-nhiem', label: 'Giáo viên Chủ Nhiệm', icon: '👩‍🏫' },
  { id: 'bo-mon', label: 'Giáo viên Bộ Môn', icon: '📚' },
  { id: 'tu-van', label: 'Đội Ngũ Tư Vấn', icon: '💬' },
];

const tuVan = [
  {
    name: 'Dương Thuý Loan',
    role: 'Tư vấn tuyển sinh',
    photo: '/duong-thuy-loan.jpg',
    mission: 'Sứ mệnh của tôi là sống trọn vẹn, có trách nhiệm và tạo ra những ký ức hạnh phúc cho gia đình và mọi người xung quanh.',
  },
  {
    name: 'Lê Thị Hoàng Yến',
    role: 'Tư vấn tuyển sinh',
    photo: '/le-thi-hoang-yen.jpg',
    mission: 'Mỗi cuộc gặp gỡ là một cơ hội để lắng nghe, kết nối và tạo ra giá trị. Tôi chọn trở thành cầu nối tin cậy để mỗi hành trình học tập được bắt đầu bằng sự an tâm, đồng hành và niềm tin.',
  },
  {
    name: 'Nguyễn Thuỵ Ngọc Thanh',
    role: 'Tư vấn tuyển sinh',
    photo: '/nguyen-thuy-ngoc-thanh.jpg',
    mission: 'Sứ mệnh của tôi là đồng hành cùng mỗi gia đình bằng sự chân thành, thấu hiểu và tận tâm, giúp phụ huynh tìm được môi trường giáo dục phù hợp để mỗi học sinh được phát triển đúng tiềm năng của mình.',
  },
  {
    name: 'Nguyễn Thị Bé Linh',
    role: 'Tư vấn tuyển sinh',
    photo: '/nguyen-thi-be-linh.jpg',
    mission: 'Sứ mệnh của tôi là trở thành người biết rõ chính mình, luôn mang năng lượng tích cực và chia sẻ niềm vui đến tất cả mọi người.',
  },
  {
    name: 'Nguyễn Thị Kim Chi',
    role: 'Tư vấn tuyển sinh',
    photo: '/nguyen-thi-kim-chi.jpg',
    mission: 'Với gia đình, tôi lấy chữ hiếu làm đầu. Với công việc, tôi giữ đạo đức và cố gắng hết mình, không chấp nhận gian dối làm tổn hại quyền lợi khách hàng. Với bản thân, tôi lấy chữ Nhẫn và chữ Thấu hiểu làm kim chỉ nam.',
  },
  {
    name: 'Nguyễn Thị Ly',
    role: 'Tư vấn tuyển sinh',
    photo: '/nguyen-thi-ly.jpg',
    mission: 'Tôi chọn yêu thương để dẫn đường, trung thực để hành động và dũng cảm để đối diện với mọi khó khăn, để lan tỏa những giá trị tốt đẹp và góp phần xây dựng một cộng đồng chân thành, nhân ái hơn.',
  },
  {
    name: 'Nguyễn Thị Minh Thu',
    role: 'Tư vấn tuyển sinh',
    photo: '/nguyen-thi-minh-thu.jpg',
    mission: 'Tôi đặt sự chân thành vào công việc, giữ tôn trọng và tự trọng trong ứng xử, để bất kỳ ai khi kết nối với tôi đều cảm thấy được lắng nghe, thấu hiểu và nhận đúng giá trị.',
  },
  {
    name: 'Trà Thị Lộc',
    role: 'Tư vấn tuyển sinh',
    photo: '/tra-thi-loc.jpg',
    mission: 'Lắng nghe để thấu hiểu – Phục vụ bằng tận tâm – Đồng hành vì sự phát triển của học sinh.',
  },
  {
    name: 'Võ Thị Xuân Yến',
    role: 'Tư vấn tuyển sinh',
    photo: '/vo-thi-xuan-yen.jpg',
    mission: 'Sứ mệnh của tôi là không ngừng học hỏi và lan tỏa những giá trị tử tế cùng tư duy tích cực đến mọi người xung quanh.',
  },
];

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Trang chủ', item: 'https://truongvietanh.com' },
    { '@type': 'ListItem', position: 2, name: 'Giới thiệu', item: 'https://truongvietanh.com/gioi-thieu' },
    { '@type': 'ListItem', position: 3, name: 'Giáo viên', item: 'https://truongvietanh.com/giao-vien' },
  ],
};

function initials(name: string): string {
  const parts = name.replace(/^(Cô|Thầy|Mr\.|Ms\.|Mrs\.)\s*/i, '').split(/\s+/);
  return parts.slice(-2).map(p => p[0]).join('').toUpperCase();
}
---

<BaseLayout
  title="Đội ngũ giáo viên – Trường Việt Anh"
  description="Giáo viên Trường Việt Anh: GV Chủ nhiệm, GV Bộ môn (Toán, Văn, Anh bản ngữ, STEAM, Thể dục, Mỹ thuật), Đội ngũ tư vấn. Đội ngũ 500+ cán bộ giàu kinh nghiệm."
  canonical="https://truongvietanh.com/giao-vien"
  ogImage="https://media.truongvietanh.com/images/thuyet-trinh-09.webp"
  structuredData={structuredData}
>
  <div class="gv-page">
    <HeaderBase />

    <!-- HERO -->
    <section class="gv-hero">
      <div class="gv-container">
        <nav class="gv-breadcrumb" aria-label="Breadcrumb">
          <a href="/">Trang chủ</a>
          <span aria-hidden="true">/</span>
          <a href="/gioi-thieu">Giới thiệu</a>
          <span aria-hidden="true">/</span>
          <span>Giáo viên</span>
        </nav>
        <h1 class="gv-h1">Đội ngũ giáo viên & tư vấn</h1>
        <p class="gv-sub">Đội ngũ giáo viên giàu kinh nghiệm, trong đó có giáo viên bản ngữ và thạc sĩ chuyên ngành — đồng hành cùng con suốt hành trình học tập.</p>
        <div class="gv-stats">
          <div class="gv-stat"><strong>{schoolFacts.yearsRunning}</strong><span>Năm kinh nghiệm</span></div>
          <div class="gv-stat"><strong>{schoolFacts.campuses}</strong><span>Cơ sở</span></div>
        </div>
      </div>
    </section>

    <!-- TABS -->
    <section class="gv-tabs-section">
      <div class="gv-tabs-bar">
        <div class="gv-container">
          <div class="gv-tabs" role="tablist" aria-label="Danh mục giáo viên">
            {tabs.map((tab, idx) => (
              <button
                class={`gv-tab ${idx === 0 ? 'is-active' : ''}`}
                role="tab"
                aria-selected={idx === 0 ? 'true' : 'false'}
                aria-controls={`panel-${tab.id}`}
                id={`tab-${tab.id}`}
                data-tab={tab.id}
                type="button"
              >
                <span class="gv-tab__icon">{tab.icon}</span>
                <span class="gv-tab__label">{tab.label}</span>
              </button>
            ))}
          </div>
        </div>
      </div>

      <div class="gv-container gv-panels-wrap">

        <!-- Panel 1: Chủ nhiệm -->
        <div class="gv-panel is-active" id="panel-chu-nhiem" role="tabpanel" aria-labelledby="tab-chu-nhiem">
          <p class="gv-panel__intro">Giáo viên chủ nhiệm trực tiếp đồng hành cùng lớp — chịu trách nhiệm về kỷ luật, liên lạc phụ huynh và kế hoạch học tập từng học sinh. <strong>Bấm vào thẻ để đọc tuyên ngôn sứ mệnh</strong> của thầy cô.</p>
          <div class="gv-grid">
            {hienThi(giaoVienChuNhiem).map(t => (
              <article class="gv-card">
                <div class="gv-card__photo">
                  {t.photo
                    ? <img src={t.photo} alt={`Ảnh ${t.name}`} loading="lazy" width="72" height="72" />
                    : <span class="gv-card__initials">{initials(t.name)}</span>
                  }
                </div>
                <div class="gv-card__body">
                  <h3 class="gv-card__name">{t.name}</h3>
                  <p class="gv-card__role">{t.role}{t.coSo ? ` · ${t.coSo}` : ''}</p>
                </div>
                {t.mission && (
                  <>
                    <button type="button" class="gv-card__nut" aria-haspopup="dialog">
                      <span class="gv-sr">Xem tuyên ngôn sứ mệnh của {t.name}</span>
                    </button>
                    {/* Chữ nằm sẵn trong HTML để bot đọc được; chỉ bị CSS che, popup lấy lại từ đây */}
                    <p class="gv-card__mission">{t.mission}</p>
                  </>
                )}
              </article>
            ))}
          </div>
        </div>

        <!-- Panel 2: Bộ môn -->
        <div class="gv-panel" id="panel-bo-mon" role="tabpanel" aria-labelledby="tab-bo-mon" hidden>
          <p class="gv-panel__intro">Giáo viên bộ môn và các vị trí chuyên môn khác — tổ trưởng chuyên môn, quản nhiệm, giáo vụ, tâm lý học đường. <strong>Bấm vào thẻ để đọc tuyên ngôn sứ mệnh</strong> của thầy cô.</p>
          <div class="gv-grid">
            {hienThi(giaoVienBoMon).map(t => (
              <article class="gv-card">
                <div class="gv-card__photo">
                  {t.photo
                    ? <img src={t.photo} alt={`Ảnh ${t.name}`} loading="lazy" width="72" height="72" />
                    : <span class="gv-card__initials">{initials(t.name)}</span>
                  }
                </div>
                <div class="gv-card__body">
                  <h3 class="gv-card__name">{t.name}</h3>
                  <p class="gv-card__role">{t.role}{t.coSo ? ` · ${t.coSo}` : ''}</p>
                </div>
                {t.mission && (
                  <>
                    <button type="button" class="gv-card__nut" aria-haspopup="dialog">
                      <span class="gv-sr">Xem tuyên ngôn sứ mệnh của {t.name}</span>
                    </button>
                    {/* Chữ nằm sẵn trong HTML để bot đọc được; chỉ bị CSS che, popup lấy lại từ đây */}
                    <p class="gv-card__mission">{t.mission}</p>
                  </>
                )}
              </article>
            ))}
          </div>
        </div>


        <div class="gv-panel" id="panel-tu-van" role="tabpanel" aria-labelledby="tab-tu-van" hidden>
          <p class="gv-panel__intro">Đội ngũ tư vấn tuyển sinh và giáo viên tư vấn học tập — hỗ trợ phụ huynh tìm hiểu về chương trình, lộ trình học và đăng ký nhập học tại tất cả cơ sở.</p>
          <div class="gv-grid gv-grid--tv">
            {tuVan.map(t => (
              <article class="gv-tvcard">
                <div class="gv-tvcard__photo">
                  {t.photo
                    ? <img src={t.photo} alt={`Ảnh ${t.name}`} loading="lazy" width="600" height="800" />
                    : <span class="gv-card__initials">{initials(t.name)}</span>
                  }
                </div>
                <div class="gv-tvcard__body">
                  <h3 class="gv-tvcard__name">{t.name}</h3>
                  <p class="gv-tvcard__role">{t.role}</p>
                  {t.mission && (
                    <div class="gv-tvcard__text">
                      <p class="gv-tvcard__quote">“{t.mission}”</p>
                    </div>
                  )}
                </div>
              </article>
            ))}
          </div>
        </div>

      </div>
    </section>

    <!-- CTA -->
    <section class="gv-cta">
      <div class="gv-container gv-cta__inner">
        <h2 class="gv-cta__h2">Muốn gặp trực tiếp giáo viên phụ trách con?</h2>
        <p class="gv-cta__desc">Đăng ký tham quan trường để gặp ban giám hiệu, GVCN dự kiến và đội ngũ tư vấn. Buổi tham quan miễn phí, kéo dài 60–90 phút.</p>
        <div class="gv-cta__actions">
          <a href="/lien-he" class="gv-btn gv-btn--primary">Đặt lịch tham quan</a>
          <a href="tel:0916961409" class="gv-btn gv-btn--ghost">Gọi 0916 961 409</a>
        </div>
      </div>
    </section>

    <!-- Popup tuyên ngôn sứ mệnh — nội dung lấy từ chính thẻ giáo viên,
         không sinh chữ mới, nên bot vẫn đọc được bản trong HTML -->
    <div class="gv-pop" id="gvPop" role="dialog" aria-modal="true" aria-labelledby="gvPopTen" hidden>
      <div class="gv-pop__nen" data-dong></div>
      <div class="gv-pop__hop" role="document">
        <button type="button" class="gv-pop__dong" data-dong aria-label="Đóng">&times;</button>
        <div class="gv-pop__anh" id="gvPopAnh" aria-hidden="true"></div>
        <h3 class="gv-pop__ten" id="gvPopTen"></h3>
        <p class="gv-pop__role" id="gvPopRole"></p>
        <p class="gv-pop__tn" id="gvPopTN"></p>
      </div>
    </div>

    <FooterBase />
  </div>
</BaseLayout>

<script is:inline>
  (function () {
    const tabs = document.querySelectorAll('.gv-tab');
    const panels = document.querySelectorAll('.gv-panel');

    function ids() {
      return Array.prototype.map.call(tabs, function (t) { return t.getAttribute('data-tab'); });
    }

    // Bat mot tab theo id (vd "tu-van"). tuCuon = true khi mo bang link co hash.
    function activate(target, tuCuon) {
      let found = null;
      tabs.forEach(function (t) {
        const on = t.getAttribute('data-tab') === target;
        if (on) found = t;
        t.classList.toggle('is-active', on);
        t.setAttribute('aria-selected', on ? 'true' : 'false');
      });
      if (!found) return false;
      panels.forEach(function (p) {
        const match = p.id === 'panel-' + target;
        p.classList.toggle('is-active', match);
        if (match) p.removeAttribute('hidden');
        else p.setAttribute('hidden', '');
      });
      if (tuCuon) {
        const wrap = document.querySelector('.gv-tabs-section') || found;
        wrap.scrollIntoView({ block: 'start' });
        // Thanh tab truot ngang tren dien thoai: keo tab dang chon vao tam nhin,
        // block:'nearest' de khong pha vi tri cuon doc vua dat o tren
        found.scrollIntoView({ block: 'nearest', inline: 'center' });
      } else {
        found.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
      }
      return true;
    }

    tabs.forEach(function (tab) {
      tab.addEventListener('click', function () {
        const target = tab.getAttribute('data-tab');
        activate(target, false);
        // Ghi vao URL de copy link duoc; replaceState nen nut Back khong bi ket trong cac tab
        if (window.history && window.history.replaceState) {
          window.history.replaceState(null, '', '#' + target);
        }
      });
    });

    // Mo dung tab khi vao bang /giao-vien#tu-van
    function tuHash(tuCuon) {
      const h = (window.location.hash || '').replace('#', '');
      if (h && ids().indexOf(h) !== -1) return activate(h, tuCuon);
      return false;
    }
    tuHash(true);
    window.addEventListener('hashchange', function () { tuHash(true); });

    // ---- Popup tuyên ngôn sứ mệnh của giáo viên ----
    var pop = document.getElementById('gvPop');
    if (pop) {
      var popAnh = document.getElementById('gvPopAnh');
      var popTen = document.getElementById('gvPopTen');
      var popRole = document.getElementById('gvPopRole');
      var popTN = document.getElementById('gvPopTN');
      var nutMo = null;

      function moPop(the, nut) {
        nutMo = nut;
        var anh = the.querySelector('.gv-card__photo img');
        var tat = the.querySelector('.gv-card__initials');
        popAnh.innerHTML = '';
        if (anh) {
          var i = document.createElement('img');
          i.src = anh.getAttribute('src');
          i.alt = '';
          popAnh.appendChild(i);
        } else if (tat) { popAnh.textContent = tat.textContent; }
        popTen.textContent = (the.querySelector('.gv-card__name') || {}).textContent || '';
        popRole.textContent = (the.querySelector('.gv-card__role') || {}).textContent || '';
        popTN.textContent = (the.querySelector('.gv-card__mission') || {}).textContent || '';
        pop.removeAttribute('hidden');
        document.body.style.overflow = 'hidden';
        pop.querySelector('.gv-pop__dong').focus();
      }

      function dongPop() {
        pop.setAttribute('hidden', '');
        document.body.style.overflow = '';
        if (nutMo) { nutMo.focus(); nutMo = null; }
      }

      document.querySelectorAll('.gv-card__nut').forEach(function (nut) {
        nut.addEventListener('click', function () { moPop(nut.closest('.gv-card'), nut); });
      });
      pop.querySelectorAll('[data-dong]').forEach(function (x) { x.addEventListener('click', dongPop); });
      document.addEventListener('keydown', function (e) {
        if (e.key === 'Escape' && !pop.hasAttribute('hidden')) dongPop();
      });
    }

    // Rut gon tuyen ngon dai o the Tu van
    var khoi = document.querySelectorAll('.gv-tvcard__text');
    if (!khoi.length) return;

    function danhGia(el) {
      // Nguoi dung da tu bam mo thi de yen, dung tu cat lai
      if (el.dataset.daMo === '1') return;
      var nut = el.parentNode.querySelector('.gv-tvcard__more');
      el.classList.add('is-clamped');
      // Chi cat khi tiet kiem duoc tren ~1,5 dong; it hon thi hien du, khoi gan nut vo nghia
      if (el.scrollHeight - el.clientHeight < 34) {
        el.classList.remove('is-clamped');
        if (nut) nut.parentNode.removeChild(nut);
        return;
      }
      if (!nut) {
        nut = document.createElement('button');
        nut.type = 'button';
        nut.className = 'gv-tvcard__more';
        nut.addEventListener('click', function () {
          var dangCat = el.classList.toggle('is-clamped');
          el.dataset.daMo = dangCat ? '0' : '1';
          nut.textContent = dangCat ? 'Xem thêm' : 'Thu gọn';
          nut.setAttribute('aria-expanded', dangCat ? 'false' : 'true');
        });
        el.parentNode.appendChild(nut);
      }
      nut.textContent = 'Xem thêm';
      nut.setAttribute('aria-expanded', 'false');
    }

    function chay() { khoi.forEach(danhGia); }

    // PHAI do SAU khi font web tai xong: do bang font du phong se ra sai so dong,
    // dan toi tuong la khong tran va khong gan nut.
    if (document.fonts && document.fonts.ready) document.fonts.ready.then(chay);
    chay();
    window.addEventListener('load', chay);
    // Doi be ngang thi so dong doi theo -> do lai
    var hen;
    window.addEventListener('resize', function () { clearTimeout(hen); hen = setTimeout(chay, 200); });
  })();
</script>

<style is:global>
  .gv-page { background: #f7f9fc; min-height: 100vh; }
  .gv-container { max-width: 1180px; margin: 0 auto; padding: 0 1.25rem; }

  /* HERO */
  .gv-hero {
    background: linear-gradient(120deg, #26275D 0%, #26275D 60%, #1e2d7a 100%);
    color: #fff; padding: 3rem 0 2.5rem;
  }
  .gv-breadcrumb { font-size: 0.85rem; color: rgba(255,255,255,.6); margin-bottom: 1rem; display: flex; gap: .5rem; flex-wrap: wrap; }
  .gv-breadcrumb a { color: #f9dd0e; text-decoration: none; }
  .gv-breadcrumb a:hover { text-decoration: underline; }
  .gv-h1 { font-family: 'Montserrat', sans-serif; font-size: clamp(1.75rem, 3.5vw, 2.4rem); font-weight: 800; color: #fff; margin: 0 0 0.75rem; line-height: 1.2; }
  .gv-sub { font-size: 1.05rem; color: rgba(255,255,255,.82); line-height: 1.65; margin: 0 0 1.75rem; max-width: 720px; }
  .gv-sub strong { color: #f9dd0e; font-weight: 700; }
  .gv-stats { display: flex; gap: 1.25rem; flex-wrap: wrap; }
  .gv-stat { display: flex; flex-direction: column; align-items: center; background: rgba(255,255,255,.08); border: 1px solid rgba(255,255,255,.12); border-radius: 12px; padding: .65rem 1.1rem; min-width: 80px; }
  .gv-stat strong { font-family: 'Montserrat', sans-serif; font-size: 1.4rem; font-weight: 900; color: #f9dd0e; line-height: 1; }
  .gv-stat span { font-size: .82rem; color: rgba(255,255,255,.7); margin-top: .2rem; text-align: center; }

  /* TABS BAR */
  .gv-tabs-bar { background: #fff; border-bottom: 2px solid #e2e8f0; position: sticky; top: 0; z-index: 100; box-shadow: 0 2px 8px rgba(0,0,0,.06); }
  .gv-tabs { display: flex; gap: 0; overflow-x: auto; scrollbar-width: none; }
  .gv-tabs::-webkit-scrollbar { display: none; }
  .gv-tab {
    background: transparent; border: none; padding: 1rem 1.25rem;
    font-family: inherit; font-size: 0.95rem; font-weight: 600; color: #64748b;
    cursor: pointer; border-bottom: 3px solid transparent; margin-bottom: -2px;
    display: inline-flex; align-items: center; gap: 0.45rem;
    white-space: nowrap; transition: color .2s, border-color .2s; flex-shrink: 0;
  }
  .gv-tab:hover { color: #26275D; }
  .gv-tab.is-active { color: #26275D; border-bottom-color: #f9dd0e; }
  .gv-tab__icon { font-size: 1.05rem; line-height: 1; }

  /* PANELS */
  .gv-panels-wrap { padding: 2rem 1.25rem 3.5rem; }
  .gv-panel { display: none; }
  .gv-panel.is-active { display: block; }
  .gv-panel__intro { font-size: 1rem; color: #475569; line-height: 1.7; margin: 0 0 1.75rem; max-width: 820px; }
  .gv-panel__intro strong { color: #26275D; font-weight: 700; }

  /* GRID */
  .gv-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
  @media (max-width: 1024px) { .gv-grid { grid-template-columns: repeat(3, 1fr); } }
  @media (max-width: 700px)  { .gv-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 480px)  { .gv-grid { grid-template-columns: 1fr; } }

  /* CARD */
  .gv-card {
    background: #fff; border: 1px solid #e2e8f0; border-radius: 14px;
    padding: 1.1rem 1rem; display: flex; gap: .85rem; align-items: center;
    transition: border-color .2s, box-shadow .2s, transform .2s;
  }
  .gv-card:hover { border-color: #f9dd0e; box-shadow: 0 8px 24px rgba(38,39,93,.09); transform: translateY(-2px); }
  .gv-card__photo {
    width: 68px; height: 68px; border-radius: 50%;
    background: linear-gradient(135deg, #26275D, #0d2f55);
    color: #fff; display: flex; align-items: center; justify-content: center;
    flex-shrink: 0; overflow: hidden;
  }
  .gv-card__photo img { width: 100%; height: 100%; object-fit: cover; object-position: top center; }
  .gv-card__initials { font-family: 'Montserrat', sans-serif; font-weight: 800; font-size: 1rem; letter-spacing: .05em; }
  .gv-card__initials--intl { background: linear-gradient(135deg, #c2410c, #ea580c); border-radius: 50%; }
  .gv-card__body { min-width: 0; }
  .gv-card__name { font-family: 'Montserrat', sans-serif; font-size: .92rem; font-weight: 700; color: #26275D; margin: 0 0 .2rem; line-height: 1.3; }
  .gv-card__role { font-size: .82rem; color: #64748b; line-height: 1.4; margin: 0; }

  /* CARD LON - chi dung cho panel Tu van */
  .gv-grid--tv { gap: 1.25rem; }
  /* 9 the anh doc: 1 cot tren dien thoai dai qua -> giu 2 cot */
  @media (max-width: 480px) { .gv-grid--tv { grid-template-columns: repeat(2, 1fr); gap: .75rem; } }
  .gv-tvcard {
    background: #fff; border: 1px solid #e2e8f0; border-radius: 14px; overflow: hidden;
    display: flex; flex-direction: column;
    transition: border-color .2s, box-shadow .2s, transform .2s;
  }
  .gv-tvcard:hover { border-color: #f9dd0e; box-shadow: 0 8px 24px rgba(38,39,93,.09); transform: translateY(-2px); }
  .gv-tvcard__photo {
    width: 100%; aspect-ratio: 3 / 4; overflow: hidden;
    background: linear-gradient(135deg, #26275D, #0d2f55);
    color: #fff; display: flex; align-items: center; justify-content: center;
  }
  .gv-tvcard__photo img { width: 100%; height: 100%; object-fit: cover; object-position: top center; display: block; }
  .gv-tvcard__photo .gv-card__initials { font-size: 2.4rem; letter-spacing: .08em; }
  .gv-tvcard__body { padding: 1rem .9rem 1.15rem; text-align: center; }
  .gv-tvcard__name { font-family: 'Montserrat', sans-serif; font-size: .98rem; font-weight: 700; color: #26275D; margin: 0 0 .25rem; line-height: 1.3; }
  .gv-tvcard__role { font-size: .8rem; color: #64748b; margin: 0 0 .7rem; }
  .gv-tvcard__quote { font-size: .84rem; font-style: italic; color: #475569; line-height: 1.6; margin: 0; }
  /* Rut gon tuyen ngon dai — chu van nam du trong HTML, chi bi CSS che */
  .gv-tvcard__text { position: relative; }
  .gv-tvcard__text.is-clamped { max-height: 6.2em; overflow: hidden; }
  .gv-tvcard__text.is-clamped::after {
    content: ''; position: absolute; left: 0; right: 0; bottom: 0; height: 2.4em;
    background: linear-gradient(to bottom, rgba(255,255,255,0), #fff 88%); pointer-events: none;
  }
  .gv-tvcard__more {
    display: block; margin: .6rem auto 0; padding: .28rem .85rem;
    font-family: inherit; font-size: .8rem; font-weight: 700; color: #26275D;
    background: rgba(249,221,14,.16); border: 1px solid rgba(38,39,93,.16); border-radius: 999px;
    cursor: pointer; transition: background .2s, border-color .2s;
  }
  .gv-tvcard__more:hover { background: #f9dd0e; border-color: #f9dd0e; }
  .gv-tvcard__more:focus-visible { outline: 2px solid #26275D; outline-offset: 2px; }
  /* THẺ BẤM ĐƯỢC + POPUP TUYÊN NGÔN */
  .gv-sr { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0; }
  /* Chữ tuyên ngôn nằm sẵn trong HTML cho bot; chỉ ẩn bằng CSS */
  .gv-card__mission { display: none; }
  .gv-card { position: relative; }
  .gv-card__nut {
    position: absolute; inset: 0; width: 100%; height: 100%;
    background: none; border: 0; padding: 0; margin: 0; cursor: pointer; border-radius: 14px;
  }
  .gv-card__nut:focus-visible { outline: 2px solid #26275D; outline-offset: 2px; }
  .gv-card:has(.gv-card__nut):hover { border-color: #f9dd0e; box-shadow: 0 8px 24px rgba(38,39,93,.09); transform: translateY(-2px); }
  /* Dấu hiệu thẻ bấm được */
  .gv-card:has(.gv-card__nut)::after {
    content: '›'; position: absolute; right: .85rem; top: 50%; transform: translateY(-50%);
    font-size: 1.35rem; line-height: 1; color: #94a3b8; transition: color .2s, transform .2s;
  }
  .gv-card:has(.gv-card__nut):hover::after { color: #26275D; transform: translateY(-50%) translateX(3px); }

  .gv-pop { position: fixed; inset: 0; z-index: 1000; display: flex; align-items: center; justify-content: center; padding: 1.25rem; }
  .gv-pop[hidden] { display: none; }
  .gv-pop__nen { position: absolute; inset: 0; background: rgba(15,23,42,.55); backdrop-filter: blur(2px); }
  .gv-pop__hop {
    position: relative; background: #fff; border-radius: 18px; max-width: 460px; width: 100%;
    max-height: 86vh; overflow-y: auto; padding: 1.75rem 1.5rem 1.5rem; text-align: center;
    box-shadow: 0 24px 60px rgba(15,23,42,.28);
  }
  .gv-pop__dong {
    position: absolute; top: .5rem; right: .65rem; width: 36px; height: 36px;
    font-size: 1.6rem; line-height: 1; color: #64748b; background: none; border: 0; cursor: pointer; border-radius: 50%;
  }
  .gv-pop__dong:hover { background: #f1f5f9; color: #26275D; }
  .gv-pop__dong:focus-visible { outline: 2px solid #26275D; outline-offset: 2px; }
  .gv-pop__anh { width: 128px; height: 128px; margin: 0 auto .9rem; border-radius: 50%; overflow: hidden;
    background: linear-gradient(135deg, #26275D, #0d2f55); color: #fff;
    display: flex; align-items: center; justify-content: center; font-family: 'Montserrat', sans-serif; font-weight: 800; font-size: 1.75rem; letter-spacing: .05em; }
  .gv-pop__anh img { width: 100%; height: 100%; object-fit: cover; object-position: top center; display: block; }
  .gv-pop__ten { font-family: 'Montserrat', sans-serif; font-size: 1.15rem; font-weight: 800; color: #26275D; margin: 0 0 .25rem; }
  .gv-pop__role { font-size: .86rem; color: #64748b; margin: 0 0 1rem; }
  .gv-pop__tn { font-size: .95rem; font-style: italic; color: #334155; line-height: 1.75; margin: 0; text-align: left; }
  .gv-pop__tn::before { content: open-quote; } .gv-pop__tn::after { content: close-quote; }
  @media (max-width: 480px) { .gv-pop__hop { padding: 1.5rem 1.1rem 1.25rem; } .gv-pop__anh { width: 104px; height: 104px; } }
  /* CTA */
  .gv-cta { background: linear-gradient(135deg, #26275D 0%, #26275D 100%); color: #fff; padding: 2.5rem 0; }
  .gv-cta__inner { text-align: center; max-width: 720px; margin: 0 auto; }
  .gv-cta__h2 { font-size: clamp(1.4rem, 2.3vw, 1.75rem); font-weight: 800; color: #fff; margin: 0 0 .75rem; }
  .gv-cta__desc { color: rgba(255,255,255,.85); font-size: 1rem; line-height: 1.65; margin: 0 0 1.25rem; }
  .gv-cta__actions { display: flex; gap: .75rem; justify-content: center; flex-wrap: wrap; }
  .gv-btn { display: inline-flex; align-items: center; justify-content: center; padding: .75rem 1.5rem; border-radius: 10px; font-weight: 700; font-size: .95rem; text-decoration: none; transition: all .2s; }
  .gv-btn--primary { background: #f9dd0e; color: #26275D; }
  .gv-btn--primary:hover { background: #f9dd0e; transform: translateY(-2px); }
  .gv-btn--ghost { background: transparent; color: #fff; border: 1.5px solid rgba(255,255,255,.6); }
  .gv-btn--ghost:hover { background: rgba(255,255,255,.12); border-color: #fff; }
</style>
