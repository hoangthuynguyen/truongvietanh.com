---
import BaseLayout from '../../layouts/BaseLayout.astro';

// ============================================================================
// SALES PAGE — 100 CÔNG DÂN TOÀN CẦU — TRƯỜNG THÁI SƠN
// URL: truongvietanh.com/truongthaison/hoc-bong-tieng-anh-99k
// Brand: Navy #000080 + CTA cam-đỏ #E63946 + Đỏ #C00000
// Mobile-first, single-file Astro page
// ============================================================================

const HERO_IMG = '/giao-vien-ban-ngu.jpg';
const KHAI_PORTRAIT = '/nguyen-duy-khai.jpg'; // placeholder if not uploaded
const HOTLINE = '0902095956';
const HOTLINE_DISPLAY = '0902 095 956';
// Cùng link Zalo OA và Facebook với toàn bộ truongvietanh.com
const ZALO_OA = 'https://zalo.me/1678310120468101523';
const FB_PAGE = 'https://www.facebook.com/truongvietanhhcm';
const FB_MESSENGER = 'https://m.me/truongvietanhhcm';
const MAP_URL = 'https://maps.app.goo.gl/LXGsk3pwninq2WKV8';
const CANONICAL = 'https://truongvietanh.com/truongthaison/hoc-bong-tieng-anh-99k';

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'EducationalOrganization',
    name: 'Trường Mầm non & Tiểu học Thái Sơn',
    url: CANONICAL,
    address: {
      '@type': 'PostalAddress',
      streetAddress: 'KDC Thái Sơn, KCN Long Hậu',
      addressLocality: 'Cần Giuộc',
      addressRegion: 'Long An',
      addressCountry: 'VN',
    },
    telephone: HOTLINE_DISPLAY,
    offers: {
      '@type': 'Offer',
      name: 'Học bổng Tiếng Anh Trẻ Em',
      price: '99000',
      priceCurrency: 'VND',
    },
  },
];

const reasons = [
  {
    icon: '🌐',
    h: 'Giáo viên nước ngoài có chứng chỉ',
    body: '1 trên 4 tiết một tuần là giáo viên bản ngữ có chứng chỉ TESOL/CELTA. Visa, bằng cấp, kinh nghiệm – tất cả công khai trên website. Lớp được chia theo trình độ và có AI cá nhân hóa lộ trình cho từng cháu.',
  },
  {
    icon: '💰',
    h: 'Học phí và học bổng',
    body: 'Cùng chương trình giáo viên nước ngoài, các trung tâm Sài Gòn thu 2,5–3,5 triệu/tháng. Tại Thái Sơn chỉ 999.000đ/tháng. Riêng 100 phụ huynh đầu tiên nhận học bổng tháng đầu chỉ 99.000đ – tiết kiệm 90% tháng đầu, 60% mỗi tháng tiếp theo.',
  },
  {
    icon: '🛡️',
    h: 'Cam kết chất lượng – 30 ngày hoàn tiền 100%',
    body: 'Cho con học thử 30 ngày. Nếu Anh/Chị không hài lòng vì BẤT KỲ lý do nào – tôi cá nhân Nguyễn Duy Khải hoàn 100% tiền học phí, không cần lý do. Anh/Chị không có gì để mất.',
  },
];

const lessons = [
  { day: 'Tiết 1 – Thứ Hai/Thứ Ba', flag: '🇬🇧', title: 'GIÁO VIÊN NƯỚC NGOÀI – KỸ NĂNG NÓI VÀ PHÁT ÂM CHUẨN', items: ['45 phút với giáo viên bản ngữ có chứng chỉ TESOL/CELTA', 'Tập trung kỹ năng nói: phát âm, ngữ điệu, sự tự tin', 'Trẻ học theo chủ đề: gia đình, nhà trường, sở thích, du lịch', 'Học liệu chuẩn quốc tế (Cambridge, Oxford)'] },
  { day: 'Tiết 2 – Thứ Tư', flag: '🇻🇳', title: 'GIÁO VIÊN VIỆT NAM – TỪ VỰNG VÀ NGỮ PHÁP CƠ BẢN', items: ['45 phút củng cố từ vựng từ tiết với giáo viên nước ngoài', 'Giảng giải ngữ pháp bằng tiếng Việt (cho trẻ chưa nền)', 'Bài tập viết, đọc hiểu cơ bản'] },
  { day: 'Tiết 3 – Thứ Sáu', flag: '🇻🇳', title: 'GIÁO VIÊN VIỆT NAM – ĐỌC HIỂU VÀ KỸ NĂNG NGHE', items: ['45 phút luyện nghe – đọc – nói tổng hợp', 'Trò chơi tiếng Anh, video ngắn, song karaoke', 'Tăng phản xạ và tình yêu tiếng Anh'] },
  { day: 'Tiết 4 – Thứ Bảy/Chủ Nhật', flag: '🇻🇳', title: 'GIÁO VIÊN VIỆT NAM – ÔN TẬP & THỰC HÀNH GIAO TIẾP', items: ['45 phút thực hành tình huống thực tế', 'Ôn tập và đánh giá tuần học', 'Trẻ trình bày trước lớp – xây sự tự tin'] },
];

const faqs = [
  { q: 'Sau tháng đầu 99.000đ, học phí tiếp theo có linh hoạt không?', a: 'Học phí 999.000đ/tháng đóng theo từng tháng, không bắt buộc đóng cả khoá. Anh/Chị có thể dừng bất cứ lúc nào, chỉ cần báo trước 7 ngày.' },
  { q: 'Có thật là có giáo viên nước ngoài không? Hay chỉ là quảng cáo?', a: 'Có thật. Trường có giáo viên bản ngữ với chứng chỉ TESOL/CELTA, visa và giấy phép lao động Việt Nam đầy đủ. Hồ sơ công khai – Anh/Chị có thể yêu cầu xem trước khi đăng ký. Anh/Chị cũng có thể đặt lịch tham quan trường để gặp trực tiếp giáo viên và xem lớp học thật.' },
  { q: 'Con tôi chưa biết tiếng Anh – có học được không?', a: 'Hoàn toàn được. Chương trình thiết kế cho trẻ chưa có nền tảng tiếng Anh. Lớp được chia theo trình độ – không theo độ tuổi – nên cháu sẽ học cùng các bạn cùng cấp độ. Trẻ mới sẽ vào lớp Trình độ 1 (Starters). Giáo viên Việt Nam biết tiếng Việt, hỗ trợ cháu trong giai đoạn đầu. Sau 4 tuần, cháu sẽ tự tin nói được câu đầu tiên.' },
  { q: 'Lớp học vào giờ nào? Tôi đi làm ca không tiện đưa đón.', a: 'Trường có nhiều khung giờ linh hoạt: Buổi tối các ngày trong tuần (17h30 hoặc 18h30), Cuối tuần (sáng/chiều Thứ Bảy, sáng/chiều Chủ Nhật). Đăng ký xong tư vấn viên sẽ liên hệ để xếp lịch phù hợp với Anh/Chị nhất.' },
  { q: 'Trường Thái Sơn nằm ở đâu cụ thể?', a: 'Khu Dân cư Thái Sơn, Khu công nghiệp Long Hậu, xã Long Hậu, huyện Cần Giuộc, tỉnh Long An. Cách KCN Long Hậu chính khoảng 5 phút đi xe, cách KCN Hiệp Phước (Nhà Bè) khoảng 10 phút đi xe.' },
  { q: 'Tôi muốn cho con học chính khoá tại Trường Thái Sơn năm sau – có được giảm thêm không?', a: 'Có. Phụ huynh tham gia chương trình tiếng Anh 99.000đ này được ưu tiên ghi danh chính khoá Mầm non/Tiểu học Thái Sơn năm 2026–2027 với học bổng giảm 50% học phí. Cụ thể: Mầm non được hưởng học bổng 50% trong 2 năm; Tiểu học được hưởng học bổng 50% đến hết tiểu học (5 năm). Tư vấn viên sẽ giới thiệu chi tiết khi Anh/Chị đăng ký.' },
];
---

<BaseLayout
  title="Học Bổng Tiếng Anh Trẻ Em 99.000đ – Trường Thái Sơn KCN Long Hậu"
  description="100 học bổng tiếng Anh trẻ em với giáo viên nước ngoài, chỉ 99.000đ tháng đầu. Tại Trường Thái Sơn, KCN Long Hậu. Cam kết hoàn tiền 30 ngày."
  canonical={CANONICAL}
  ogImage="https://media.truongvietanh.com/images/extra-giao-vien-va-hoc-sinh-2.webp"
  structuredData={structuredData}
  noPopup={true}
>
  <main class="ts-page">

    <!-- ============ HERO ============ -->
    <section class="ts-hero">
      <div class="ts-container ts-hero__inner">
        <p class="ts-hero__pre">🎓 100 SUẤT HỌC BỔNG ĐẶC BIỆT – TRƯỜNG THÁI SƠN</p>
        <h1 class="ts-hero__h1">
          Tiếng Anh Có Giáo Viên Nước Ngoài
          <span class="ts-hero__price">Chỉ <em>99.000đ</em> Tháng Đầu</span>
        </h1>
        <ul class="ts-trust">
          <li>
            <span class="ts-trust__icon">🏛️</span>
            <span class="ts-trust__title">15 năm Việt Anh</span>
            <span class="ts-trust__desc">đào tạo học sinh giỏi chuyên Anh tại TP.HCM</span>
          </li>
          <li>
            <span class="ts-trust__icon">🌐</span>
            <span class="ts-trust__title">Giáo viên bản ngữ</span>
            <span class="ts-trust__desc">có chứng chỉ TESOL / CELTA</span>
          </li>
          <li>
            <span class="ts-trust__icon">🤖</span>
            <span class="ts-trust__title">AI cá nhân hóa</span>
            <span class="ts-trust__desc">hiệu suất học tập tăng x2</span>
          </li>
          <li>
            <span class="ts-trust__icon">🛡️</span>
            <span class="ts-trust__title">Hoàn tiền 30 ngày</span>
            <span class="ts-trust__desc">không cần lý do</span>
          </li>
        </ul>
        <a href="#dang-ky" class="ts-cta-btn ts-cta-btn--xl ts-cta-track" data-cta="hero">
          Nhận học bổng ngay — chỉ còn <span data-seats>23</span> suất
        </a>
        <p class="ts-hero__scarcity">⏰ Cập nhật theo thời gian thực: <span data-registered>52</span> / 100 phụ huynh đã đăng ký</p>
      </div>
    </section>

    <!-- ============ VIDEO STANDALONE ============ -->
    <section class="ts-video-section">
      <div class="ts-container">
        <div class="ts-video-wrap">
          <video
            class="ts-video"
            controls
            preload="metadata"
            playsinline
            poster={KHAI_PORTRAIT}
          >
            <source src="https://media.truongvietanh.com/videos/%237%20%E2%80%93%20T%C3%94I%20T%C3%8AN%20L%C3%80%20NGUY%E1%BB%84N%20DUY%20KH%E1%BA%A2I.mp4" type="video/mp4" />
            Trình duyệt của Anh/Chị không hỗ trợ video HTML5.
          </video>
        </div>
        <p class="ts-video-caption">▶ Anh Nguyễn Duy Khải – Giám đốc Trường Thái Sơn giới thiệu chương trình</p>
      </div>
    </section>

    <!-- ============ SOCIAL PROOF ============ -->
    <section class="ts-proof">
      <div class="ts-container">
        <p class="ts-proof__eyebrow">ĐƯỢC VẬN HÀNH BỞI HỆ THỐNG TRƯỜNG VIỆT ANH<br/>15 NĂM ĐÀO TẠO HỌC SINH GIỎI CHUYÊN ANH TẠI TP.HCM</p>
        <div class="ts-proof__counters">
          <div><strong>15</strong><span>NĂM KINH NGHIỆM VIỆT ANH</span></div>
          <div><strong>100</strong><span>SUẤT HỌC BỔNG</span></div>
          <div><strong>4</strong><span>TIẾT/TUẦN</span></div>
          <div><strong>30</strong><span>NGÀY HOÀN TIỀN VÔ ĐIỀU KIỆN</span></div>
        </div>
      </div>
    </section>

    <!-- ============ STORY ============ -->
    <section class="ts-story" id="cau-chuyen">
      <div class="ts-container ts-story__grid">
        <div class="ts-story__photo">
          <img
            src={KHAI_PORTRAIT}
            alt="Anh Nguyễn Duy Khải — Giám đốc Trường Thái Sơn"
            class="ts-story__photo-img"
            loading="lazy"
            width="320"
            height="320"
          />
          <p class="ts-story__sig">
            <em>— Nguyễn Duy Khải</em><br/>
            Regional Manager đương nhiệm – Major Education<br/>
            Giám đốc Trường Mầm non Thái Sơn, Tiểu học Thái Sơn, Mầm non Nhân Lễ
          </p>
        </div>
        <div class="ts-story__body">
          <h2>Vì Sao Một Regional Manager Đương Nhiệm Major Education Lại Mở Trường Ở KCN Long Hậu?</h2>
          <p class="ts-story__sub"><em>Một câu chuyện cá nhân của Anh Nguyễn Duy Khải</em></p>
          <p>Tôi tên là Nguyễn Duy Khải.</p>
          <p>Tôi từng là một đứa trẻ tỉnh lẻ. Vào Sài Gòn lập nghiệp tay trắng. Tôi học tiếng Anh bằng mọi cách – đêm nào cũng nghe BBC, ngày nào cũng ép mình nói chuyện với người nước ngoài dù phát âm sai.</p>
          <p>Tiếng Anh đã đổi đời tôi. Hôm nay tôi đang là Regional Manager của Major Education – một trong những hệ thống giáo dục tiếng Anh lớn nhất Việt Nam.</p>
          <p>Nhưng có một điều tôi không thể quên: con của những phụ huynh ở khu công nghiệp như Long Hậu, Hiệp Phước – họ không có cơ hội như con nhà khá giả Sài Gòn để học tiếng Anh chuẩn từ nhỏ. Một lớp tiếng Anh giáo viên nước ngoài 2,5–3,5 triệu một tháng là quá sức với phụ huynh công nhân.</p>
          <p>Vì vậy tôi mở Trường Thái Sơn ngay trong KDC Thái Sơn, KCN Long Hậu – được vận hành bởi Hệ thống Trường Việt Anh, đơn vị đã có 15 năm kinh nghiệm đào tạo học sinh giỏi chuyên Anh tại TP.HCM. Chúng tôi mang chương trình tiếng Anh giáo viên nước ngoài kết hợp ứng dụng trí tuệ nhân tạo cá nhân hóa lộ trình học – cùng chất lượng tôi vận hành cho con nhà khá giả – về cho khu vực này, chỉ 999.000 đồng một tháng. Tháng đầu chỉ 99.000 đồng để Anh/Chị thử trước.</p>
          <p><strong>Tôi muốn con của Anh/Chị có cơ hội như tôi đã từng có.</strong></p>
        </div>
      </div>
    </section>

    <!-- ============ 3 LÝ DO ============ -->
    <section class="ts-reasons">
      <div class="ts-container">
        <h2 class="ts-h2-center">3 Lý Do Phụ Huynh KCN Long Hậu Đã Chọn Chương Trình Này</h2>
        <div class="ts-reasons__grid ts-reasons__grid--3">
          {reasons.map(r => (
            <article class="ts-card">
              <div class="ts-card__icon">{r.icon}</div>
              <h3>{r.h}</h3>
              <p>{r.body}</p>
            </article>
          ))}
        </div>
        <div class="ts-center">
          <a href="#dang-ky" class="ts-cta-btn ts-cta-btn--xl ts-cta-track" data-cta="reasons">
            Đăng ký nhận học bổng ngay
          </a>
        </div>
      </div>
    </section>

    <!-- ============ CHƯƠNG TRÌNH HỌC ============ -->
    <section class="ts-program">
      <div class="ts-container">
        <h2 class="ts-h2-center">Trong 4 Tiết Một Tuần, Con Bạn Sẽ Học Gì?</h2>
        <p class="ts-h2-sub">Chương trình thiết kế bởi đội ngũ chuyên gia có nhiều năm kinh nghiệm tại Major Education và các hệ thống giáo dục tiếng Anh hàng đầu Việt Nam.</p>
        <div class="ts-program__list">
          {lessons.map(l => (
            <article class="ts-lesson">
              <div class="ts-lesson__head">
                <span class="ts-lesson__day">{l.day}</span>
              </div>
              <h3 class="ts-lesson__title"><span class="ts-flag">{l.flag}</span>{l.title}</h3>
              <ul>
                {l.items.map(i => <li>{i}</li>)}
              </ul>
            </article>
          ))}
        </div>
        <div class="ts-program__info">
          <div class="ts-info-row"><span>👥</span><div><strong>SĨ SỐ MỖI LỚP: TỐI ĐA 20 TRẺ</strong><br/>Đảm bảo giáo viên có thời gian cho từng cháu</div></div>
          <div class="ts-info-row"><span>📊</span><div><strong>CHIA LỚP THEO TRÌNH ĐỘ – KHÔNG THEO ĐỘ TUỔI:</strong><ul class="ts-info-sub"><li>Trình độ 1 – Starters (mới bắt đầu, làm quen tiếng Anh)</li><li>Trình độ 2 – Movers (đã biết chữ cái, từ vựng cơ bản)</li><li>Trình độ 3 – Flyers (đã có nền tảng giao tiếp đơn giản)</li><li>Trình độ 4 – Pre-A2 (chuẩn bị nâng cao)</li></ul>Mỗi cháu được test đầu vào miễn phí trước khi xếp lớp.</div></div>
          <div class="ts-info-row"><span>🤖</span><div><strong>ỨNG DỤNG TRÍ TUỆ NHÂN TẠO:</strong><br/>Hệ thống AI cá nhân hóa lộ trình học cho từng cháu, đề xuất bài tập phù hợp, nhắc ôn tập đúng thời điểm – giúp hiệu suất học tập tăng gấp 2 lần.</div></div>
          <div class="ts-info-row"><span>📚</span><div><strong>GIÁO TRÌNH:</strong> Cambridge / Oxford – chuẩn quốc tế</div></div>
        </div>
      </div>
    </section>

    <!-- ============ BÁO GIÁ 3-TIER ============ -->
    <section class="ts-pricing">
      <div class="ts-container">
        <h2 class="ts-h2-center">Học Phí Chương Trình Tiếng Anh Trẻ Em</h2>
        <p class="ts-h2-sub">Cùng chương trình giáo viên nước ngoài, AI cá nhân hóa, 4 tiết / tuần.</p>
        <div class="ts-pricing__grid">
          <article class="ts-price-card ts-price-card--regular">
            <p class="ts-price-card__label">Giá trị tương đương</p>
            <p class="ts-price-card__amount"><s>2.500.000<span class="ts-price-card__unit">VND/tháng</span></s></p>
            <p class="ts-price-card__note">Mức học phí trung bình của các trung tâm tiếng Anh có giáo viên nước ngoài tại Sài Gòn (Apollo, ILA, VUS).</p>
          </article>
          <article class="ts-price-card ts-price-card--standard">
            <p class="ts-price-card__label">Giá ưu đãi tại Thái Sơn</p>
            <p class="ts-price-card__amount">999.000<span class="ts-price-card__unit">VND/tháng</span></p>
            <p class="ts-price-card__note">Mức học phí thường xuyên áp dụng cho phụ huynh KCN Long Hậu — tiết kiệm ~60% so với trung tâm Sài Gòn.</p>
          </article>
          <article class="ts-price-card ts-price-card--featured">
            <p class="ts-price-card__badge">100 SUẤT HỌC BỔNG ĐẦU TIÊN</p>
            <p class="ts-price-card__label">Học bổng Công Dân Toàn Cầu</p>
            <p class="ts-price-card__amount ts-price-card__amount--featured">99.000<span class="ts-price-card__unit">VND/tháng</span></p>
            <p class="ts-price-card__note">Áp dụng cho 100 phụ huynh đầu tiên đăng ký — tiết kiệm 90% so với giá trung tâm Sài Gòn. Tháng đầu thử trước, có quyền rút khỏi chương trình mà không mất phí.</p>
            <a href="#dang-ky" class="ts-cta-btn ts-cta-btn--full ts-cta-track" data-cta="pricing">Nhận học bổng 99.000đ</a>
          </article>
        </div>
        <p class="ts-pricing__note">* Giá tham khảo các trung tâm tại TP.HCM, cập nhật tháng 4/2026. Học phí Thái Sơn đóng theo từng tháng, không cần đóng cả khoá.</p>
      </div>
    </section>

    <!-- ============ CAM KẾT HOÀN TIỀN ============ -->
    <section class="ts-guarantee">
      <div class="ts-container ts-guarantee__grid">
        <div class="ts-guarantee__portrait">
          <img src={KHAI_PORTRAIT} alt="Anh Nguyễn Duy Khải — Giám đốc Trường Thái Sơn" loading="lazy" width="320" height="320" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
          <div class="ts-guarantee__portrait-fallback" aria-hidden="true">
            <span>Anh Nguyễn Duy Khải</span>
          </div>
          <p class="ts-guarantee__sig">
            <em>— Nguyễn Duy Khải</em><br/>
            Giám đốc Trường Thái Sơn<br/>
            Regional Manager – Major Education
          </p>
        </div>
        <div class="ts-guarantee__body">
          <p class="ts-guarantee__shield" aria-hidden="true">🛡️</p>
          <h2>Tôi cá nhân cam kết với Anh / Chị</h2>
          <p>Cho con học thử <em>30 ngày</em> tại Trường Thái Sơn.</p>
          <p>Sau 30 ngày, nếu Anh / Chị không hài lòng vì bất kỳ lý do nào — con không vui đến lớp, giáo viên không phù hợp, lịch học bất tiện, hay đơn giản là Anh / Chị đổi ý — tôi sẽ hoàn <em>100% tiền học phí</em>. Không cần lý do. Không có câu hỏi.</p>
          <p>Tôi đứng tên cá nhân: Nguyễn Duy Khải. Nếu tôi sai, Anh / Chị đến tận trường tìm tôi.</p>
          <p class="ts-guarantee__lead">99.000 đồng — Anh / Chị không có gì để mất.</p>
        </div>
      </div>
    </section>

    <!-- ============ FAQ ============ -->
    <section class="ts-faq">
      <div class="ts-container">
        <h2 class="ts-h2-center">Câu Hỏi Anh/Chị Hay Hỏi</h2>
        <div class="ts-faq__list">
          {faqs.map((f, i) => (
            <details class="ts-faq__item" open={i === 0}>
              <summary>{f.q}</summary>
              <p>{f.a}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- ============ COUNTDOWN + SCARCITY ============ -->
    <section class="ts-scarcity">
      <div class="ts-container ts-center">
        <p class="ts-scarcity__eyebrow">⏰ HỌC BỔNG NÀY KHÔNG KÉO DÀI</p>
        <div class="ts-scarcity__counters">
          <div><strong data-seats>23</strong><span>SUẤT CÒN LẠI</span></div>
          <div><strong>7</strong><span>NGÀY ĐẾN HẠN</span></div>
          <div><strong>99.000Đ</strong><span>THÁNG ĐẦU</span></div>
        </div>
        <p class="ts-scarcity__sub"><em>Khi đủ 100 phụ huynh đăng ký, chúng tôi đóng nhận hồ sơ. Sau đó học phí trở về mức thông thường 999.000đ/tháng – sẽ <strong>KHÔNG</strong> còn mức 99.000đ.</em></p>
        <a href="#dang-ky" class="ts-cta-btn ts-cta-btn--xl ts-cta-track" data-cta="scarcity">GIỮ CHỖ NGAY – CHỈ MẤT 30 GIÂY</a>
      </div>
    </section>

    <!-- ============ FORM ĐĂNG KÝ ============ -->
    <section class="ts-form-section" id="dang-ky">
      <div class="ts-container ts-form-section__grid">
        <div class="ts-form-col">
          <h2 class="ts-h2-center">Đăng Ký Học Bổng Tiếng Anh 99.000Đ</h2>
          <p class="ts-form__sub-eyebrow"><em>Chỉ mất 30 giây – 2 bước đơn giản</em></p>

          <div class="ts-form-card">
            <div class="ts-progress" data-step="1">
              <div class="ts-progress__bar"><div class="ts-progress__fill"></div></div>
              <p class="ts-progress__label"><span class="ts-step-now">Bước 1: Email</span> → <span class="ts-step-next">Bước 2: Tên & SĐT</span></p>
            </div>

            <form id="tsForm" novalidate>
              <!-- Step 1 -->
              <div class="ts-step ts-step--1" data-step-pane="1">
                <h3 class="ts-form-h3">Bước 1/2 – Để lại email để giữ chỗ</h3>
                <p class="ts-form-help">Chúng tôi sẽ gửi thông tin chi tiết chương trình + voucher giữ chỗ học bổng 99.000đ trong vòng 1 phút.</p>
                <label for="tsEmail" class="ts-label">Email của Anh/Chị <span class="ts-req">*</span></label>
                <input id="tsEmail" name="email" type="email" required placeholder="vd: nguyenthilan@gmail.com" autocomplete="email" />
                <button type="submit" class="ts-cta-btn ts-cta-btn--xl ts-cta-btn--full">GIỮ CHỖ HỌC BỔNG NGAY →</button>
                <p class="ts-form__trust">🔒 Email Anh/Chị được bảo mật theo Nghị định 13/2023/NĐ-CP. Chúng tôi <strong>KHÔNG</strong> gửi spam.</p>
              </div>

              <!-- Step 2 -->
              <div class="ts-step ts-step--2" data-step-pane="2" hidden>
                <div class="ts-urgency-banner">⚡ KHẨN CẤP: 3 phụ huynh khác vừa đăng ký trong 5 phút qua. Hoàn tất bước 2 để giữ chỗ chắc chắn cho con!</div>
                <h3 class="ts-form-h3">Bước 2/2 – Hoàn tất thông tin liên hệ</h3>
                <p class="ts-form-help">Tư vấn viên sẽ liên hệ Anh/Chị trong 5 phút (giờ hành chính) hoặc qua Zalo trước 9h sáng mai (ngoài giờ).</p>
                <label for="tsName" class="ts-label">Họ và tên Phụ huynh <span class="ts-req">*</span></label>
                <input id="tsName" name="name" type="text" required placeholder="Vd: Nguyễn Thị Lan" autocomplete="name" />
                <label for="tsPhone" class="ts-label">Số điện thoại <span class="ts-req">*</span></label>
                <input id="tsPhone" name="phone" type="tel" required placeholder="0909 xxx xxx" autocomplete="tel" inputmode="tel" />
                <label class="ts-checkbox">
                  <input type="checkbox" name="consent" checked />
                  <span>Tôi đồng ý nhận thông tin về chương trình qua điện thoại, Zalo, email</span>
                </label>
                <button type="submit" class="ts-cta-btn ts-cta-btn--xl ts-cta-btn--full">HOÀN TẤT ĐĂNG KÝ – NHẬN HỌC BỔNG 99.000Đ →</button>
              </div>

              <!-- Success block -->
              <div class="ts-step ts-success" data-step-pane="success" hidden>
                <div class="ts-success__icon" aria-hidden="true">⚠️</div>
                <h3 class="ts-success__title">ĐĂNG KÝ ĐANG TĂNG TỪNG GIỜ – CHAT NGAY ĐỂ KHÔNG MẤT SUẤT!</h3>
                <p class="ts-success__body">Cảm ơn Anh/Chị! Thông tin đã được ghi nhận.<br/><br/>NHƯNG: 100 suất học bổng đang được phụ huynh khu vực đăng ký liên tục. Trong khi tư vấn viên gọi lại Anh/Chị (5 phút), có thể có 2-3 phụ huynh khác đã chốt suất.<br/><br/>Để CHẮC CHẮN giữ được suất cho con của Anh/Chị, hãy CHAT NGAY với chúng tôi qua Zalo hoặc Messenger – tư vấn viên đang chờ phản hồi Anh/Chị trong vòng 30 giây.</p>
                <div class="ts-success__ctas">
                  <a href={`${ZALO_OA}?msg=${encodeURIComponent('Tôi vừa đăng ký học bổng 99k cho con - muốn chat ngay')}`} target="_blank" rel="noopener" class="ts-cta-btn ts-cta-btn--zalo ts-cta-btn--full">💬 CHAT NGAY QUA ZALO – PHẢN HỒI 30 GIÂY</a>
                  <a href={`${FB_MESSENGER}?ref=hoc_bong_99k`} target="_blank" rel="noopener" class="ts-cta-btn ts-cta-btn--fb ts-cta-btn--full">💬 Chat ngay qua Messenger</a>
                  <a href={`tel:${HOTLINE}`} class="ts-cta-btn ts-cta-btn--outline ts-cta-btn--full">📞 GỌI HOTLINE NGAY: {HOTLINE_DISPLAY}</a>
                </div>
                <p class="ts-success__counter">🔥 Vừa có thêm 1 phụ huynh đăng ký – còn <strong data-seats>22</strong> suất</p>
              </div>
            </form>
          </div>
        </div>

        <!-- Benefits col -->
        <aside class="ts-benefits">
          <div class="ts-benefits__box">
            <h3>🎁 KHI ANH/CHỊ ĐĂNG KÝ HÔM NAY, CON SẼ NHẬN:</h3>
            <ul>
              <li><strong>4 tiết tiếng Anh tháng đầu chỉ 99.000đ</strong><br/>(giá gốc 999.000đ – giảm 90%)</li>
              <li><strong>Test trình độ đầu vào miễn phí</strong><br/>để xếp đúng lớp – không bị nhồi nhét theo tuổi</li>
              <li><strong>Lộ trình học cá nhân hóa bằng AI</strong><br/>(hiệu suất học tập x2)</li>
              <li><strong>Ưu tiên ghi danh chính khoá năm 2026-2027</strong><br/>với học bổng 50% học phí cả khoá</li>
              <li><strong>Cam kết hoàn tiền 100% trong 30 ngày</strong><br/>nếu không hài lòng</li>
            </ul>
            <div class="ts-benefits__total">
              <p>TỔNG GIÁ TRỊ: <s>999.000Đ</s></p>
              <p class="ts-benefits__price">ANH/CHỊ TRẢ: chỉ <strong>99.000Đ</strong></p>
            </div>
            <p class="ts-benefits__trust">🔒 SSL Secured · 📞 Hotline: <a href={`tel:${HOTLINE}`}>{HOTLINE_DISPLAY}</a> · 💬 <a href={ZALO_OA} target="_blank" rel="noopener">Zalo OA</a></p>
          </div>
        </aside>
      </div>
    </section>

    <!-- ============ TESTIMONIAL PLACEHOLDER ============ -->
    <section class="ts-testimonial">
      <div class="ts-container ts-center">
        <h2 class="ts-h2-center">Phụ Huynh Đang Nói Gì Về Chương Trình?</h2>
        <p class="ts-testimonial__placeholder"><em>Khu vực này sẽ cập nhật testimonial từ phụ huynh đăng ký sớm trong những ngày đầu của chương trình.</em></p>
      </div>
    </section>

    <!-- ============ LIÊN HỆ ============ -->
    <section class="ts-contact">
      <div class="ts-container">
        <h2 class="ts-h2-center">Liên hệ trực tiếp</h2>
        <p class="ts-h2-sub">Tư vấn 24/7 — chúng tôi luôn sẵn sàng giải đáp.</p>
        <div class="ts-contact__cards">
          <a href={`tel:${HOTLINE}`} class="ts-contact-card" data-cta="contact-phone">
            <span class="ts-contact-card__icon">📞</span>
            <span class="ts-contact-card__label">Hotline</span>
            <span class="ts-contact-card__value">{HOTLINE_DISPLAY}</span>
            <span class="ts-contact-card__sub">24/7 — không giới hạn thời gian</span>
          </a>
          <a href={ZALO_OA} target="_blank" rel="noopener" class="ts-contact-card" data-cta="contact-zalo">
            <span class="ts-contact-card__icon">💬</span>
            <span class="ts-contact-card__label">Zalo OA</span>
            <span class="ts-contact-card__value">Trường Việt Anh</span>
            <span class="ts-contact-card__sub">Phản hồi trong vòng 1 phút</span>
          </a>
          <a href={FB_PAGE} target="_blank" rel="noopener" class="ts-contact-card" data-cta="contact-fb">
            <span class="ts-contact-card__icon">📘</span>
            <span class="ts-contact-card__label">Facebook</span>
            <span class="ts-contact-card__value">@truongvietanhhcm</span>
            <span class="ts-contact-card__sub">Theo dõi tin tức &amp; sự kiện</span>
          </a>
        </div>
        <div class="ts-contact__address">
          <p><span aria-hidden="true">📍</span> Trường Mầm non &amp; Tiểu học Thái Sơn — KDC Thái Sơn, KCN Long Hậu, xã Long Hậu, huyện Cần Giuộc, tỉnh Long An</p>
          <a href={MAP_URL} target="_blank" rel="noopener" class="ts-cta-btn ts-cta-btn--outline">Xem trên Google Maps</a>
        </div>
      </div>
    </section>

    <!-- ============ STICKY MOBILE CTA ============ -->
    <div class="ts-sticky" id="tsSticky" hidden>
      <button class="ts-sticky__close" aria-label="Đóng" id="tsStickyClose">×</button>
      <div class="ts-sticky__inner">
        <span class="ts-sticky__count">CÒN <strong data-seats>23</strong> SUẤT</span>
        <a href="#dang-ky" class="ts-sticky__cta ts-cta-track" data-cta="sticky">GIỮ CHỖ – 99.000Đ</a>
      </div>
    </div>

    <!-- ============ FOOTER ============ -->
    <footer class="ts-footer">
      <div class="ts-container ts-footer__grid">
        <div>
          <p class="ts-footer__brand"><strong>Hệ thống Trường Việt Anh</strong></p>
          <p>Trường Mầm non &amp; Tiểu học Thái Sơn<br/>Thuộc Hệ thống Trường Việt Anh</p>
          <p><em>Vì một thế hệ Công Dân Toàn Cầu</em></p>
        </div>
        <div>
          <p class="ts-footer__heading">THÔNG TIN</p>
          <ul>
            <li><a href="/gioi-thieu">Về Trường Thái Sơn</a></li>
            <li><a href="/">Hệ thống Trường Việt Anh</a></li>
            <li><a href="/blog">Tin tức &amp; Sự kiện</a></li>
          </ul>
        </div>
        <div>
          <p class="ts-footer__heading">Liên hệ Mr Khải</p>
          <ul>
            <li>Hotline 24/7: <a href={`tel:${HOTLINE}`}>{HOTLINE_DISPLAY}</a></li>
            <li>Zalo OA: <a href={ZALO_OA} target="_blank" rel="noopener">Trường Việt Anh</a></li>
            <li>Facebook: <a href={FB_PAGE} target="_blank" rel="noopener">@truongvietanhhcm</a></li>
            <li>Email: <a href="mailto:khai.nguyen@truongvietanh.com">khai.nguyen@truongvietanh.com</a></li>
          </ul>
        </div>
        <div>
          <p class="ts-footer__heading">ĐỊA CHỈ</p>
          <p>KDC Thái Sơn, KCN Long Hậu<br/>Cần Giuộc, Long An<br/><a href={MAP_URL} target="_blank" rel="noopener">📍 Xem bản đồ</a></p>
        </div>
      </div>
      <div class="ts-footer__bottom">
        <p>© 2026 Hệ thống Trường Việt Anh. All rights reserved. <a href="https://truongvietanh.com">truongvietanh.com</a></p>
      </div>
    </footer>

  </main>
</BaseLayout>

<style is:global>
  /* ============================================================================
     SALES PAGE — TRƯỜNG THÁI SƠN — 100 CÔNG DÂN TOÀN CẦU
     Palette: Navy #000080 / Vàng brand #FFFF00 (mute var(--ts-yellow-mute) trên nền tối, #FFF7CC nền sáng)
              / CTA cam-đỏ #E63946 / Đỏ #C00000
     Mobile-first, font tối thiểu 16px body / 18px button / 24px H3
     ============================================================================ */
  .ts-page{
    --ts-navy: #000080;
    --ts-navy-dk: #00005a;
    --ts-blue: #1A4FB3;
    --ts-cta: #E63946;
    --ts-cta-dk: #B81D2A;
    --ts-cta-hl: #FF6B35;
    --ts-red: #C00000;
    --ts-yellow: #FFFF00;
    --ts-yellow-mute: #FFE066;
    --ts-yellow-soft: #FFF7CC;
    --ts-yellow-softer: #FFFCEB;
    --ts-bg: #F7F9FC;
    --ts-text: #1A1A1A;
    --ts-sub: #4A4A4A;
    --ts-border: #E5E7EB;
    --ts-white: #FFFFFF;
    color: var(--ts-text);
    font-family: 'Inter', 'Open Sans', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    font-size: 16px;
    line-height: 1.65;
  }
  .ts-page h1, .ts-page h2, .ts-page h3, .ts-page h4 {
    font-family: 'Montserrat', system-ui, sans-serif;
    font-weight: 700;
    color: var(--ts-navy);
    line-height: 1.2;
  }
  /* Hero text white on navy bg */
  .ts-page .ts-hero h1,
  .ts-page .ts-hero h2,
  .ts-page .ts-hero h3 { color: #fff; }
  .ts-page .ts-scarcity h1,
  .ts-page .ts-scarcity h2,
  .ts-page .ts-scarcity p { color: #fff; }
  .ts-page .ts-success h2,
  .ts-page .ts-success h3 { color: var(--ts-red); }
  .ts-page p { margin: 0 0 1rem; }
  .ts-page a { color: var(--ts-navy); }
  .ts-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  @media (min-width: 768px) {
    .ts-container { padding: 0 2rem; }
  }
  .ts-center { text-align: center; }

  .ts-h2-center {
    font-size: 28px;
    text-align: center;
    margin: 0 0 .5rem;
    font-weight: 700;
  }
  @media (min-width: 768px) { .ts-h2-center { font-size: 34px; } }
  .ts-h2-sub {
    text-align: center;
    color: var(--ts-sub);
    font-size: 17px;
    max-width: 720px;
    margin: 0 auto 2.5rem;
    line-height: 1.65;
  }

  /* ====== CTA BUTTONS — brand yellow #FFFF00 + navy text ====== */
  .ts-page a.ts-cta-btn,
  .ts-page button.ts-cta-btn,
  .ts-cta-btn {
    display: inline-block;
    background: var(--ts-yellow);
    color: var(--ts-navy);
    font-weight: 700;
    text-decoration: none;
    border: none;
    border-radius: 10px;
    padding: 14px 26px;
    font-size: 18px;
    letter-spacing: .01em;
    transition: transform .15s ease, box-shadow .15s ease, filter .15s ease;
    box-shadow: 0 4px 16px rgba(0, 0, 0, .18), 0 0 0 1px rgba(0, 0, 128, .08);
    cursor: pointer;
    line-height: 1.3;
  }
  .ts-cta-btn:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0, 0, 0, .22), 0 0 0 1px rgba(0, 0, 128, .12); filter: brightness(1.02); }
  .ts-cta-btn:active { transform: translateY(0); box-shadow: 0 3px 10px rgba(0, 0, 0, .18); }
  .ts-cta-btn--xl { font-size: 19px; padding: 18px 34px; }
  .ts-cta-btn--full { width: 100%; display: block; text-align: center; }
  .ts-cta-btn--zalo { background: #0068FF; box-shadow: 0 4px 14px rgba(0, 104, 255, .35); }
  .ts-cta-btn--zalo:hover { background: #0050C7; }
  .ts-cta-btn--fb { background: #1877F2; box-shadow: 0 4px 14px rgba(24, 119, 242, .35); }
  .ts-cta-btn--fb:hover { background: #115DC1; }
  .ts-page a.ts-cta-btn--outline,
  .ts-cta-btn--outline {
    background: transparent; color: var(--ts-navy);
    border: 2px solid var(--ts-navy); box-shadow: none;
  }
  .ts-cta-btn--outline:hover { background: var(--ts-navy); color: #fff; filter: none; }

  /* ====== HERO ====== */
  .ts-hero {
    position: relative;
    background:
      linear-gradient(115deg, rgba(0,0,128,.94) 0%, rgba(0,0,90,.84) 55%, rgba(0,0,128,.7) 100%),
      url('/giao-vien-ban-ngu.jpg') center / cover no-repeat,
      var(--ts-navy);
    color: #fff;
    padding: 48px 0 56px;
    overflow: hidden;
  }
  @media (min-width: 768px) { .ts-hero { padding: 72px 0 80px; } }
  .ts-hero__bg, .ts-hero__overlay { display: none; }
  .ts-hero__inner { position: relative; max-width: 920px; margin: 0 auto; text-align: center; }
  .ts-hero__pre {
    color: var(--ts-yellow-mute);
    font-size: 14px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .12em;
    margin: 0 0 1.25rem;
  }
  @media (min-width: 768px) { .ts-hero__pre { font-size: 15px; } }
  .ts-hero__h1 {
    color: #fff;
    font-size: 30px;
    font-weight: 700;
    line-height: 1.2;
    margin: 0 0 2rem;
    letter-spacing: -.01em;
  }
  @media (min-width: 768px) { .ts-hero__h1 { font-size: 44px; } }
  .ts-hero__price {
    display: block;
    margin-top: .5rem;
    font-size: 30px;
    font-weight: 700;
    color: #fff;
    line-height: 1.2;
  }
  @media (min-width: 768px) { .ts-hero__price { font-size: 44px; } }
  .ts-hero__price em {
    font-style: normal;
    color: var(--ts-navy);
    background: var(--ts-yellow);
    padding: .05em .35em;
    border-radius: 8px;
    font-weight: 700;
    margin: 0 .12em;
    display: inline-block;
    box-shadow: 0 2px 12px rgba(0, 0, 0, .15);
  }

  /* Trust badges: white card with red+blue text */
  .ts-trust {
    list-style: none; padding: 0;
    margin: 0 auto 2rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: .65rem;
    max-width: 720px;
    text-align: left;
  }
  @media (min-width: 640px) { .ts-trust { grid-template-columns: repeat(4, 1fr); gap: .85rem; } }
  .ts-trust li {
    display: flex; flex-direction: column; align-items: flex-start; gap: .15rem;
    background: #fff;
    border-radius: 12px;
    padding: .85rem .9rem;
    box-shadow: 0 4px 16px rgba(0, 0, 0, .12);
  }
  .ts-trust__icon { font-size: 22px; line-height: 1; margin-bottom: .25rem; }
  .ts-trust__title { font-size: 15px; font-weight: 700; color: var(--ts-red); line-height: 1.2; }
  .ts-trust__desc { font-size: 13px; color: var(--ts-blue); line-height: 1.4; }
  @media (min-width: 768px) {
    .ts-trust__title { font-size: 16px; }
    .ts-trust__desc { font-size: 14px; }
  }

  .ts-hero__scarcity {
    color: rgba(255,255,255,.92);
    font-size: 15px;
    margin: 1.25rem 0 0;
  }
  @media (min-width: 768px) { .ts-hero__scarcity { font-size: 16px; } }
  .ts-hero__scarcity span { color: var(--ts-yellow-mute); font-weight: 600; }

  /* ====== VIDEO STANDALONE ====== */
  .ts-video-section { background: var(--ts-bg); padding: 40px 0; }
  @media (min-width: 768px) { .ts-video-section { padding: 56px 0; } }
  .ts-video-section .ts-container { max-width: 760px; }
  .ts-video-wrap {
    aspect-ratio: 16 / 9;
    background: #000;
    border-radius: 14px;
    overflow: hidden;
    position: relative;
    box-shadow: 0 12px 40px rgba(0, 0, 128, .15);
  }
  .ts-video-placeholder {
    position: absolute; inset: 0;
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    gap: .5rem; color: rgba(255,255,255,.85);
    background: linear-gradient(135deg, rgba(0,0,128,.85), rgba(0,0,90,.85)),
      url('/giao-vien-ban-ngu.jpg') center / cover no-repeat;
  }
  .ts-video-play {
    width: 76px; height: 76px;
    background: var(--ts-cta);
    color: #fff;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 30px; padding-left: 6px;
    box-shadow: 0 6px 24px rgba(230, 57, 70, .45);
    transition: transform .2s ease;
  }
  .ts-video-wrap:hover .ts-video-play { transform: scale(1.08); }
  .ts-video {
    position: absolute; inset: 0;
    width: 100%; height: 100%;
    object-fit: contain; display: block;
    background: #000;
  }
  .ts-video-text { font-size: 14px; opacity: .85; }
  .ts-video-caption { color: var(--ts-sub); font-size: 16px; margin: 1rem 0 0; text-align: center; }

  /* ====== SOCIAL PROOF ====== */
  .ts-proof { background: var(--ts-white); padding: 40px 0; }
  .ts-proof__eyebrow {
    text-align: center; color: var(--ts-sub);
    font-size: 16px; margin: 0 0 1.5rem; line-height: 1.5;
    font-weight: 600; text-transform: uppercase; letter-spacing: .04em;
  }
  .ts-proof__counters {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;
  }
  @media (min-width: 768px) { .ts-proof__counters { grid-template-columns: repeat(4, 1fr); } }
  .ts-proof__counters > div { text-align: center; }
  .ts-proof__counters strong {
    display: block; font-family: 'Montserrat', sans-serif;
    font-size: 40px; font-weight: 800; color: var(--ts-navy); line-height: 1;
  }
  @media (min-width: 768px) { .ts-proof__counters strong { font-size: 48px; } }
  .ts-proof__counters span {
    display: block; font-size: 16px; color: var(--ts-sub);
    margin-top: .35rem; font-weight: 600;
  }

  /* ====== STORY ====== */
  .ts-story { background: var(--ts-bg); padding: 60px 0; }
  @media (min-width: 768px) { .ts-story { padding: 80px 0; } }
  .ts-story__grid {
    display: grid; grid-template-columns: 1fr; gap: 2rem; align-items: start;
  }
  @media (min-width: 1024px) { .ts-story__grid { grid-template-columns: 320px 1fr; gap: 3rem; } }
  .ts-story__photo-img {
    width: 100%; height: auto;
    object-fit: contain;
    border-radius: 12px;
    border: 3px solid var(--ts-yellow-mute);
    display: block;
    background: #f7f9fc;
    box-shadow: 0 8px 24px rgba(0, 0, 128, .15);
  }
  .ts-story__sig {
    font-size: 16px; color: var(--ts-sub); margin-top: 1rem;
    line-height: 1.6;
  }
  .ts-story__sig em { color: var(--ts-navy); font-weight: 700; font-style: normal; font-size: 18px; }
  .ts-story__body h2 {
    font-size: 28px; margin: 0 0 .5rem;
  }
  @media (min-width: 768px) { .ts-story__body h2 { font-size: 36px; } }
  .ts-story__sub { color: var(--ts-sub); font-size: 18px; margin-bottom: 1.5rem; }
  .ts-story__body p { font-size: 16px; line-height: 1.75; }
  @media (min-width: 768px) { .ts-story__body p { font-size: 18px; } }

  /* ====== REASONS ====== */
  .ts-reasons { background: var(--ts-white); padding: 56px 0; }
  @media (min-width: 768px) { .ts-reasons { padding: 80px 0; } }
  .ts-reasons__grid {
    display: grid; grid-template-columns: 1fr; gap: 1.25rem;
    margin: 2rem 0 2.5rem;
  }
  @media (min-width: 1024px) {
    .ts-reasons__grid--3 { grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  }
  .ts-card {
    background: #fff; border: 1px solid var(--ts-border);
    padding: 1.75rem 1.5rem; border-radius: 14px;
    transition: box-shadow .2s ease, transform .2s ease, border-color .2s ease;
    display: flex; flex-direction: column;
  }
  .ts-card:hover {
    box-shadow: 0 12px 32px rgba(0, 0, 128, .1);
    transform: translateY(-3px);
    border-color: var(--ts-navy);
  }
  .ts-card__icon { font-size: 38px; margin-bottom: .75rem; line-height: 1; }
  .ts-card h3 { font-size: 22px; margin: 0 0 .5rem; color: var(--ts-navy); font-weight: 700; line-height: 1.3; }
  @media (min-width: 768px) { .ts-card h3 { font-size: 24px; } }
  .ts-card p { font-size: 16px; line-height: 1.7; color: var(--ts-text); margin: 0; }

  /* ====== PROGRAM ====== */
  .ts-program { background: var(--ts-bg); padding: 60px 0; }
  @media (min-width: 768px) { .ts-program { padding: 80px 0; } }
  .ts-program__list {
    display: grid; gap: 1rem; margin: 2rem 0;
  }
  .ts-lesson {
    background: #fff; border-left: 4px solid var(--ts-navy);
    padding: 1.5rem; border-radius: 0 8px 8px 0;
    box-shadow: 0 2px 8px rgba(0,0,0,.06);
  }
  .ts-lesson__day {
    display: inline-block; background: var(--ts-navy); color: #fff;
    font-size: 14px; font-weight: 700; padding: .25rem .75rem;
    border-radius: 50px; margin-bottom: .5rem; text-transform: uppercase; letter-spacing: .05em;
  }
  .ts-lesson__title { font-size: 18px; margin: 0 0 .75rem; line-height: 1.3; }
  @media (min-width: 768px) { .ts-lesson__title { font-size: 20px; } }
  .ts-flag { margin-right: .5rem; }
  .ts-lesson ul { padding-left: 1.25rem; margin: 0; font-size: 16px; line-height: 1.65; color: var(--ts-text); }
  .ts-lesson li { margin-bottom: .25rem; }

  .ts-program__info {
    background: var(--ts-yellow-softer);
    border: 1px solid var(--ts-yellow-mute);
    border-radius: 14px;
    padding: 1.5rem;
    margin-top: 2rem;
  }
  @media (min-width: 768px) { .ts-program__info { padding: 2rem; } }
  .ts-info-row {
    display: flex; align-items: flex-start; gap: 1rem;
    padding: .85rem 0;
    border-bottom: 1px solid rgba(0,0,128,.08);
  }
  .ts-info-row:last-child { border-bottom: none; }
  .ts-info-row > span { font-size: 22px; flex-shrink: 0; line-height: 1.3; }
  .ts-info-row > div { font-size: 16px; line-height: 1.65; }
  .ts-info-row strong { color: var(--ts-red); font-weight: 700; }
  .ts-info-sub { padding-left: 1.25rem; margin: .5rem 0; }
  .ts-info-sub li { font-size: 16px; line-height: 1.65; }

  /* ====== PRICING 3-TIER ====== */
  .ts-pricing { background: var(--ts-white); padding: 56px 0; }
  @media (min-width: 768px) { .ts-pricing { padding: 80px 0; } }
  .ts-pricing__grid {
    display: grid; grid-template-columns: 1fr; gap: 1.25rem;
    margin: 1.5rem 0 1rem;
  }
  @media (min-width: 768px) {
    .ts-pricing__grid { grid-template-columns: repeat(3, 1fr); gap: 1.25rem; align-items: stretch; }
  }
  .ts-price-card {
    background: #fff;
    border: 1px solid var(--ts-border);
    border-radius: 16px;
    padding: 1.75rem 1.5rem;
    text-align: center;
    display: flex; flex-direction: column;
    transition: transform .2s ease, box-shadow .2s ease;
  }
  .ts-price-card--regular { opacity: .85; }
  .ts-price-card--standard { border-color: var(--ts-navy); }
  .ts-price-card--featured {
    background: linear-gradient(180deg, #FFFCEB 0%, #FFF7CC 100%);
    border: 2px solid var(--ts-cta);
    box-shadow: 0 12px 32px rgba(230, 57, 70, .18);
    position: relative;
    transform: scale(1.02);
  }
  @media (min-width: 768px) {
    .ts-price-card--featured { transform: scale(1.04); }
  }
  .ts-price-card:hover { transform: translateY(-2px); }
  .ts-price-card--featured:hover { transform: translateY(-4px) scale(1.04); }
  .ts-price-card__badge {
    display: inline-block; align-self: center;
    background: var(--ts-cta); color: #fff;
    font-size: 13px; font-weight: 700;
    padding: .35rem .85rem; border-radius: 999px;
    text-transform: uppercase; letter-spacing: .04em;
    margin-bottom: 1rem;
  }
  .ts-price-card__label {
    font-size: 14px; color: var(--ts-sub);
    text-transform: uppercase; letter-spacing: .06em;
    margin: 0 0 .65rem;
    font-weight: 600;
  }
  .ts-price-card--featured .ts-price-card__label { color: var(--ts-red); }
  .ts-price-card__amount {
    font-family: 'Montserrat', sans-serif;
    font-size: 32px; font-weight: 700;
    color: var(--ts-navy); line-height: 1;
    margin: 0 0 .85rem;
  }
  @media (min-width: 768px) { .ts-price-card__amount { font-size: 36px; } }
  .ts-price-card__amount--featured { color: var(--ts-red); font-size: 38px; }
  @media (min-width: 768px) { .ts-price-card__amount--featured { font-size: 44px; } }
  .ts-price-card__amount s { color: var(--ts-sub); }
  .ts-price-card__unit {
    display: block; font-size: 14px; font-weight: 500;
    color: var(--ts-sub); margin-top: .25rem; letter-spacing: 0;
  }
  .ts-price-card__note {
    font-size: 14px; color: var(--ts-sub); line-height: 1.6;
    margin: 0 0 1rem;
  }
  .ts-price-card .ts-cta-btn { margin-top: auto; }
  .ts-pricing__note { font-size: 13px; color: var(--ts-sub); text-align: center; margin: 1rem 0 0; }

  /* ====== GUARANTEE ====== */
  .ts-guarantee { background: var(--ts-yellow-softer); padding: 56px 0; }
  @media (min-width: 768px) { .ts-guarantee { padding: 72px 0; } }
  .ts-guarantee__grid {
    display: grid; grid-template-columns: 1fr; gap: 2rem; align-items: center;
    max-width: 980px; margin: 0 auto;
  }
  @media (min-width: 1024px) {
    .ts-guarantee__grid { grid-template-columns: 280px 1fr; gap: 3rem; }
  }
  .ts-guarantee__portrait { text-align: center; }
  .ts-guarantee__portrait img {
    width: 100%; max-width: 260px; aspect-ratio: 1/1;
    object-fit: cover; object-position: center 18%;
    border-radius: 50%;
    border: 5px solid #fff;
    box-shadow: 0 12px 32px rgba(0, 0, 128, .15);
    display: block; margin: 0 auto;
  }
  .ts-guarantee__portrait-fallback {
    display: none; /* hidden until image fails */
    width: 220px; height: 220px;
    margin: 0 auto;
    background: linear-gradient(135deg, var(--ts-navy), #2a2a8a);
    color: rgba(255,255,255,.92);
    border-radius: 50%; align-items: center; justify-content: center;
    text-align: center; padding: 1rem; font-size: 16px;
    border: 5px solid #fff;
    box-shadow: 0 12px 32px rgba(0, 0, 128, .15);
  }
  .ts-guarantee__sig {
    font-size: 15px; color: var(--ts-sub); line-height: 1.6;
    margin: 1rem 0 0;
  }
  .ts-guarantee__sig em { color: var(--ts-navy); font-weight: 700; font-style: normal; font-size: 17px; }
  .ts-guarantee__shield { font-size: 48px; line-height: 1; margin: 0 0 .5rem; }
  .ts-guarantee__body h2 {
    font-size: 26px; color: var(--ts-red); margin: 0 0 1.25rem;
    font-weight: 700;
  }
  @media (min-width: 768px) { .ts-guarantee__body h2 { font-size: 32px; } }
  .ts-guarantee__body p { font-size: 17px; line-height: 1.7; margin: 0 0 .85rem; color: var(--ts-text); }
  .ts-guarantee__body p em {
    font-style: normal;
    background: #fff;
    color: var(--ts-red);
    padding: 0 .25em;
    border-radius: 4px;
    font-weight: 700;
  }
  .ts-guarantee__lead {
    font-size: 20px !important;
    color: var(--ts-red) !important;
    font-weight: 700;
    margin-top: 1rem !important;
  }

  /* ====== FAQ ====== */
  .ts-faq { background: var(--ts-white); padding: 56px 0; }
  @media (min-width: 768px) { .ts-faq { padding: 72px 0; } }
  .ts-faq__list {
    max-width: 820px; margin: 1.5rem auto 0;
    display: flex; flex-direction: column; gap: .75rem;
  }
  .ts-faq__item {
    background: #fff;
    border: 1px solid var(--ts-border);
    border-radius: 10px;
    overflow: hidden;
    transition: border-color .2s, box-shadow .2s;
  }
  .ts-faq__item[open] { border-color: var(--ts-navy); box-shadow: 0 4px 16px rgba(0, 0, 128, .08); }
  .ts-faq__item summary {
    list-style: none; cursor: pointer;
    padding: 1rem 1.25rem;
    font-weight: 600; font-size: 17px; color: var(--ts-navy);
    display: flex; justify-content: space-between; align-items: center;
    user-select: none;
    line-height: 1.4;
  }
  .ts-faq__item summary::-webkit-details-marker { display: none; }
  .ts-faq__item summary::after {
    content: '+'; font-size: 22px; flex-shrink: 0;
    color: var(--ts-cta); margin-left: 1rem; transition: transform .2s;
    font-weight: 400;
  }
  .ts-faq__item[open] summary::after { content: '−'; }
  .ts-faq__item p {
    padding: 0 1.25rem 1.25rem; margin: 0;
    font-size: 16px; line-height: 1.7; color: var(--ts-text);
  }

  /* ====== SCARCITY ====== */
  .ts-scarcity {
    background: linear-gradient(135deg, var(--ts-navy-dk) 0%, var(--ts-navy) 100%);
    color: #fff; padding: 56px 0;
  }
  .ts-scarcity__eyebrow {
    color: var(--ts-yellow-mute);
    font-size: 16px; font-weight: 600;
    text-transform: uppercase; letter-spacing: .1em;
    margin: 0 0 1.5rem;
  }
  @media (min-width: 768px) { .ts-scarcity__eyebrow { font-size: 18px; } }
  .ts-scarcity__counters {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: .85rem;
    max-width: 720px; margin: 0 auto 1.5rem;
  }
  @media (min-width: 768px) { .ts-scarcity__counters { gap: 1rem; } }
  .ts-scarcity__counters > div {
    background: rgba(255,255,255,.08); border: 1px solid rgba(255,255,255,.16);
    border-radius: 12px; padding: 1.25rem .5rem;
  }
  .ts-scarcity__counters strong {
    display: block; font-family: 'Montserrat', sans-serif;
    font-size: 32px; font-weight: 700; color: var(--ts-yellow-mute); line-height: 1;
  }
  @media (min-width: 768px) { .ts-scarcity__counters strong { font-size: 52px; } }
  .ts-scarcity__counters span {
    display: block; margin-top: .35rem;
    font-size: 12px; color: rgba(255,255,255,.85);
    text-transform: uppercase; letter-spacing: .04em;
    line-height: 1.4;
  }
  @media (min-width: 768px) { .ts-scarcity__counters span { font-size: 14px; } }
  .ts-scarcity__sub {
    max-width: 680px; margin: 0 auto 1.75rem;
    font-size: 16px; color: rgba(255,255,255,.88); line-height: 1.6;
  }
  .ts-scarcity__sub strong { color: var(--ts-yellow-mute); }

  /* ====== FORM SECTION ====== */
  .ts-form-section { background: var(--ts-white); padding: 60px 0; scroll-margin-top: 60px; }
  @media (min-width: 768px) { .ts-form-section { padding: 80px 0; } }
  .ts-form-section__grid {
    display: grid; grid-template-columns: 1fr; gap: 2rem;
  }
  @media (min-width: 1024px) { .ts-form-section__grid { grid-template-columns: 60% 40%; gap: 3rem; align-items: start; } }
  .ts-form__sub-eyebrow { text-align: center; color: var(--ts-sub); font-size: 16px; margin: 0 0 1.5rem; }

  .ts-form-card {
    background: #fff;
    border: 2px solid var(--ts-border);
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 4px 24px rgba(0,0,0,.06);
  }
  @media (min-width: 768px) { .ts-form-card { padding: 2rem; } }
  .ts-progress { margin-bottom: 1.5rem; }
  .ts-progress__bar { height: 8px; background: var(--ts-border); border-radius: 4px; overflow: hidden; margin-bottom: .5rem; }
  .ts-progress__fill {
    height: 100%; width: 50%;
    background: linear-gradient(90deg, var(--ts-navy), var(--ts-blue));
    transition: width .3s ease;
  }
  .ts-progress[data-step="2"] .ts-progress__fill { width: 100%; }
  .ts-progress__label { font-size: 14px; color: var(--ts-sub); text-align: center; margin: 0; }
  .ts-progress[data-step="2"] .ts-step-now { color: var(--ts-sub); }
  .ts-progress[data-step="2"] .ts-step-next { color: var(--ts-navy); font-weight: 700; }
  .ts-progress[data-step="1"] .ts-step-now { color: var(--ts-navy); font-weight: 700; }

  .ts-form-h3 { font-size: 24px; margin: 0 0 .5rem; }
  .ts-form-help { font-size: 16px; color: var(--ts-sub); margin: 0 0 1rem; line-height: 1.6; }
  .ts-label { display: block; font-size: 16px; font-weight: 600; margin: 1rem 0 .35rem; color: var(--ts-text); }
  .ts-req { color: var(--ts-red); }
  .ts-page input[type="email"],
  .ts-page input[type="tel"],
  .ts-page input[type="text"] {
    display: block; width: 100%;
    padding: 14px 16px;
    border: 2px solid var(--ts-border);
    border-radius: 8px;
    font-size: 18px;
    font-family: inherit;
    color: var(--ts-text); background: #fff;
    transition: border-color .15s ease;
  }
  .ts-page input:focus {
    outline: none; border-color: var(--ts-navy);
    box-shadow: 0 0 0 3px rgba(0,0,128,.15);
  }
  .ts-page input.ts-input-error { border-color: var(--ts-red); }

  .ts-checkbox {
    display: flex; align-items: flex-start; gap: .5rem;
    margin: 1rem 0 1.25rem; font-size: 16px; color: var(--ts-text);
    line-height: 1.5; cursor: pointer;
  }
  .ts-checkbox input { margin-top: .25rem; transform: scale(1.2); }

  .ts-form__trust { font-size: 14px; color: var(--ts-sub); text-align: center; margin: 1rem 0 0; line-height: 1.5; }
  .ts-step { animation: tsFadeIn .3s ease; }
  @keyframes tsFadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }

  .ts-urgency-banner {
    background: #FFE5E5; color: var(--ts-red);
    padding: 1rem; border-radius: 8px;
    text-align: center; font-size: 16px; font-weight: 600;
    margin-bottom: 1.25rem; line-height: 1.5;
  }

  /* Success block */
  .ts-success {
    background: var(--ts-yellow-softer);
    border: 2px solid var(--ts-cta);
    border-radius: 14px;
    padding: 2rem 1.5rem;
    text-align: center;
  }
  @media (min-width: 768px) { .ts-success { padding: 2.5rem 2rem; } }
  .ts-success__icon { font-size: 56px; line-height: 1; margin-bottom: 1rem; }
  .ts-success__title { font-size: 22px; color: var(--ts-red); margin: 0 0 1rem; line-height: 1.3; font-weight: 700; }
  @media (min-width: 768px) { .ts-success__title { font-size: 26px; } }
  .ts-success__body { font-size: 16px; line-height: 1.75; color: var(--ts-text); margin: 0 0 1.5rem; }
  .ts-success__ctas { display: flex; flex-direction: column; gap: .75rem; }
  .ts-success__counter {
    margin-top: 1.25rem; font-size: 15px; color: var(--ts-red); font-weight: 600;
    animation: tsBlink 1.6s infinite;
  }
  @keyframes tsBlink { 0%, 100% { opacity: 1; } 50% { opacity: .55; } }

  /* Benefits */
  .ts-benefits__box {
    background: var(--ts-yellow-softer);
    border: 1px solid var(--ts-yellow-mute);
    border-radius: 14px;
    padding: 1.5rem;
    box-shadow: 0 4px 16px rgba(255, 224, 102, .15);
  }
  @media (min-width: 768px) { .ts-benefits__box { padding: 2rem; } }
  .ts-benefits__box h3 {
    font-size: 18px; color: var(--ts-red);
    margin: 0 0 1rem; font-weight: 700;
    line-height: 1.35;
  }
  .ts-benefits__box ul { list-style: none; padding: 0; margin: 0 0 1.5rem; display: flex; flex-direction: column; gap: .85rem; }
  .ts-benefits__box li {
    font-size: 15px; line-height: 1.6;
    padding-left: 1.65rem; position: relative;
    color: var(--ts-text);
  }
  .ts-benefits__box li::before {
    content: '✓'; position: absolute; left: 0; top: 0;
    color: var(--ts-cta); font-weight: 700;
  }
  .ts-benefits__box li strong { color: var(--ts-navy); font-weight: 600; }
  .ts-benefits__total {
    background: #fff; border-radius: 10px;
    padding: 1rem; text-align: center; margin-bottom: 1rem;
    border: 1px solid var(--ts-border);
  }
  .ts-benefits__total p { margin: 0; font-size: 14px; color: var(--ts-sub); font-weight: 500; }
  .ts-benefits__total p s { color: var(--ts-sub); }
  .ts-benefits__price { font-size: 16px !important; color: var(--ts-text) !important; margin-top: .35rem !important; }
  .ts-benefits__price strong { color: var(--ts-cta); font-size: 24px; font-weight: 700; }
  .ts-benefits__trust { text-align: center; font-size: 13px; color: var(--ts-sub); margin: 0; line-height: 1.6; }

  /* ====== TESTIMONIAL ====== */
  .ts-testimonial { background: var(--ts-bg); padding: 60px 0; }
  .ts-testimonial__placeholder {
    max-width: 540px; margin: 1.5rem auto 0;
    font-size: 16px; color: var(--ts-sub); line-height: 1.7;
    background: #fff; padding: 2rem;
    border: 2px dashed var(--ts-border);
    border-radius: 12px;
  }

  /* ====== CONTACT ====== */
  .ts-contact { background: var(--ts-white); padding: 56px 0; }
  @media (min-width: 768px) { .ts-contact { padding: 72px 0; } }
  .ts-contact__cards {
    display: grid; grid-template-columns: 1fr; gap: 1rem;
    margin: 1rem 0 2rem;
  }
  @media (min-width: 768px) { .ts-contact__cards { grid-template-columns: repeat(3, 1fr); gap: 1.25rem; } }
  .ts-contact-card {
    background: #fff;
    border: 1px solid var(--ts-border);
    border-radius: 14px;
    padding: 1.5rem 1.25rem;
    text-decoration: none;
    text-align: center;
    transition: transform .2s ease, box-shadow .2s ease, border-color .2s ease;
    display: flex; flex-direction: column; align-items: center;
    color: var(--ts-text);
  }
  .ts-contact-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 28px rgba(0, 0, 128, .12);
    border-color: var(--ts-navy);
  }
  .ts-contact-card__icon { font-size: 32px; line-height: 1; margin-bottom: .5rem; }
  .ts-contact-card__label { font-size: 13px; color: var(--ts-sub); text-transform: uppercase; letter-spacing: .06em; margin-bottom: .35rem; }
  .ts-contact-card__value { font-size: 18px; color: var(--ts-navy); font-weight: 700; margin-bottom: .35rem; }
  .ts-contact-card__sub { font-size: 13px; color: var(--ts-sub); }
  .ts-contact__address {
    text-align: center; max-width: 720px; margin: 0 auto;
    padding: 1.5rem; background: var(--ts-bg); border-radius: 12px;
  }
  .ts-contact__address p { font-size: 16px; color: var(--ts-text); line-height: 1.65; margin: 0 0 1rem; }
  .ts-contact__address p span { color: var(--ts-cta); }

  /* ====== STICKY MOBILE CTA ====== */
  .ts-sticky {
    position: fixed; bottom: 0; left: 0; right: 0;
    z-index: 9999;
    background: linear-gradient(135deg, var(--ts-cta-hl) 0%, var(--ts-cta) 100%);
    padding: 10px 12px;
    box-shadow: 0 -4px 16px rgba(0,0,0,.18);
    display: flex; align-items: center; gap: .5rem;
  }
  @media (min-width: 1024px) { .ts-sticky { display: none !important; } }
  .ts-sticky__close {
    background: rgba(255,255,255,.2); border: none; cursor: pointer;
    font-size: 20px; line-height: 1; color: #fff;
    padding: 4px 10px; border-radius: 6px;
    flex-shrink: 0;
  }
  .ts-sticky__inner {
    flex: 1; display: flex; align-items: center; gap: .5rem;
    justify-content: space-between;
  }
  .ts-sticky__count { font-size: 13px; color: #fff; font-weight: 600; }
  .ts-sticky__count strong { color: var(--ts-yellow-mute); font-size: 16px; font-weight: 700; }
  .ts-sticky__cta {
    background: #fff; color: var(--ts-cta);
    font-weight: 700; font-size: 14px;
    padding: 10px 14px; border-radius: 8px;
    text-decoration: none;
    flex-shrink: 0;
    white-space: nowrap;
  }

  /* ====== FOOTER ====== */
  .ts-footer {
    background: linear-gradient(180deg, var(--ts-navy-dk) 0%, var(--ts-navy) 100%);
    color: #fff; padding: 56px 0 1rem;
  }
  .ts-footer p, .ts-footer li { color: #fff; }
  .ts-footer a { color: var(--ts-yellow-mute); text-decoration: none; }
  .ts-footer a:hover { color: #fff; text-decoration: underline; }
  .ts-footer__grid {
    display: grid; grid-template-columns: 1fr; gap: 2rem;
  }
  @media (min-width: 640px) { .ts-footer__grid { grid-template-columns: 1fr 1fr; } }
  @media (min-width: 1024px) { .ts-footer__grid { grid-template-columns: repeat(4, 1fr); } }
  .ts-footer__heading {
    color: var(--ts-yellow-mute); font-weight: 700;
    text-transform: uppercase; letter-spacing: .06em;
    font-size: 14px; margin: 0 0 .75rem;
  }
  .ts-footer__brand { color: #fff; font-size: 17px; margin: 0 0 .5rem; font-weight: 700; }
  .ts-footer p { font-size: 15px; line-height: 1.65; margin: 0 0 .5rem; }
  .ts-footer ul { list-style: none; padding: 0; margin: 0; }
  .ts-footer li { font-size: 15px; margin-bottom: .35rem; line-height: 1.5; }
  .ts-footer__bottom {
    border-top: 1px solid rgba(255,255,255,.15);
    margin-top: 2.5rem; padding: 1rem 0 0;
    text-align: center; font-size: 13px; color: rgba(255,255,255,.85);
  }
  .ts-footer__bottom p { margin: 0; color: rgba(255,255,255,.85); font-size: 13px; }

  /* ====== ANIMATIONS — fade in on scroll ====== */
  .ts-anim-init { opacity: 0; transform: translateY(20px); transition: opacity .35s ease, transform .35s ease; }
  .ts-anim-show { opacity: 1; transform: translateY(0); }
</style>

<script is:inline>
(function () {
  const HOTLINE = '0902095956';
  const ZALO_OA = 'https://zalo.me/1678310120468101523';
  const FB_PAGE = 'https://m.me/truongvietanhhcm';
  const LEAD_API_URL = '/api/lead';
  const SEATS_API_URL = '/api/seats-remaining';

  // ====== Scarcity counter — fetch from API, fallback static ======
  async function loadSeats() {
    try {
      const res = await fetch(SEATS_API_URL);
      if (!res.ok) throw new Error('seat api failed');
      const data = await res.json();
      const seats = data.seats || data.remaining || 23;
      document.querySelectorAll('[data-seats]').forEach(el => { el.textContent = seats; });
      const reg = (data.registered || (100 - seats));
      document.querySelectorAll('[data-registered]').forEach(el => { el.textContent = reg; });
    } catch {
      // fallback already in DOM
    }
  }
  loadSeats();
  setInterval(loadSeats, 30000);

  // ====== Scroll fade-in ======
  const animEls = document.querySelectorAll('.ts-page section, .ts-card, .ts-lesson');
  animEls.forEach(el => el.classList.add('ts-anim-init'));
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('ts-anim-show');
        io.unobserve(e.target);
      }
    });
  }, { threshold: 0.08 });
  animEls.forEach(el => io.observe(el));

  // ====== Sticky mobile CTA: show after scroll past hero ======
  const sticky = document.getElementById('tsSticky');
  const stickyClose = document.getElementById('tsStickyClose');
  let stickyDismissed = sessionStorage.getItem('ts_sticky_closed') === '1';
  function checkSticky() {
    if (stickyDismissed) return;
    const past = window.scrollY > window.innerHeight * 0.9;
    sticky.hidden = !past;
  }
  window.addEventListener('scroll', checkSticky, { passive: true });
  checkSticky();
  stickyClose && stickyClose.addEventListener('click', () => {
    sticky.hidden = true; stickyDismissed = true;
    sessionStorage.setItem('ts_sticky_closed', '1');
  });

  // ====== CTA click tracking ======
  document.querySelectorAll('.ts-cta-track').forEach(btn => {
    btn.addEventListener('click', () => {
      const loc = btn.dataset.cta || 'unknown';
      if (typeof window.fbq === 'function') {
        window.fbq('trackCustom', 'CTAClick', { cta_location: loc });
      }
      if (typeof window.gtag === 'function') {
        window.gtag('event', 'cta_click', { cta_location: loc });
      }
    });
  });

  // ====== Scroll halfway tracking ======
  let scrollHalfFired = false;
  window.addEventListener('scroll', () => {
    if (scrollHalfFired) return;
    const pct = (window.scrollY + window.innerHeight) / document.documentElement.scrollHeight;
    if (pct >= 0.5) {
      scrollHalfFired = true;
      if (typeof window.fbq === 'function') window.fbq('trackCustom', 'ScrollHalf');
      if (typeof window.gtag === 'function') window.gtag('event', 'scroll_50_percent');
    }
  }, { passive: true });

  // ====== FORM 2-step ======
  const form = document.getElementById('tsForm');
  if (!form) return;
  const progress = form.parentElement.querySelector('.ts-progress');
  const step1 = form.querySelector('[data-step-pane="1"]');
  const step2 = form.querySelector('[data-step-pane="2"]');
  const success = form.querySelector('[data-step-pane="success"]');
  const emailInput = form.querySelector('#tsEmail');
  const nameInput = form.querySelector('#tsName');
  const phoneInput = form.querySelector('#tsPhone');

  // UTM
  const params = new URLSearchParams(window.location.search);
  const utm = {
    utm_source: params.get('utm_source') || '',
    utm_medium: params.get('utm_medium') || '',
    utm_campaign: params.get('utm_campaign') || '',
    utm_content: params.get('utm_content') || '',
    utm_term: params.get('utm_term') || '',
  };

  let leadData = { ...utm };

  function showStep(n) {
    step1.hidden = n !== 1;
    step2.hidden = n !== 2;
    success.hidden = n !== 'success';
    progress.dataset.step = n === 'success' ? 2 : n;
    if (n === 2 && nameInput) setTimeout(() => nameInput.focus(), 100);
  }

  function validEmail(v) { return /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(v.trim()); }
  function validPhone(v) { return /^(0|\+84)[3-9][0-9]{8}$/.test(v.replace(/[\s.\-]/g, '')); }

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    const currentPane = step1.hidden ? (step2.hidden ? 'success' : 2) : 1;

    if (currentPane === 1) {
      const email = emailInput.value.trim();
      if (!validEmail(email)) {
        emailInput.classList.add('ts-input-error');
        emailInput.focus();
        return;
      }
      emailInput.classList.remove('ts-input-error');
      leadData.email = email;
      // Fire intermediate Lead event
      if (typeof window.fbq === 'function') {
        window.fbq('trackCustom', 'StepOneCompleted', { value: 99000, currency: 'VND' });
      }
      // Send partial lead
      submitLead({ ...leadData, partial: true, funnel_code: 'ts_99k' }).catch(() => {});
      showStep(2);
      return;
    }

    if (currentPane === 2) {
      const name = nameInput.value.trim();
      const phone = phoneInput.value.trim().replace(/[\s.\-]/g, '');
      let bad = false;
      if (!name || name.length < 2) { nameInput.classList.add('ts-input-error'); bad = true; } else nameInput.classList.remove('ts-input-error');
      if (!validPhone(phone)) { phoneInput.classList.add('ts-input-error'); bad = true; } else phoneInput.classList.remove('ts-input-error');
      if (bad) return;

      leadData.name = name;
      leadData.phone = phone;
      leadData.funnel_code = 'ts_99k';
      leadData.school_level = 'tieu-hoc';
      leadData.tag = 'Need_immediate_chat';

      const submitBtn = step2.querySelector('button[type="submit"]');
      submitBtn.disabled = true;
      submitBtn.textContent = 'ĐANG XỬ LÝ...';

      try {
        await submitLead(leadData);
      } catch (err) {
        console.warn('Lead submit error', err);
      }

      // Fire conversion events
      if (typeof window.fbq === 'function') {
        window.fbq('track', 'Lead', {
          value: 99000, currency: 'VND',
          content_name: '100_cong_dan_toan_cau_99k',
        });
      }
      if (typeof window.gtag === 'function') {
        window.gtag('event', 'generate_lead', { currency: 'VND', value: 99000 });
      }
      if (typeof window.ttq !== 'undefined' && window.ttq.track) {
        window.ttq.track('SubmitForm');
      }

      showStep('success');
    }
  });

  async function submitLead(payload) {
    return fetch(LEAD_API_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
    });
  }
})();
</script>
