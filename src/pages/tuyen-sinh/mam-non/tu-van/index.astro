---
/* Pain Point Solver Landing Page */
const pageTitle = 'Con hay khóc khi đi học? 5 sai lầm thường gặp khi chọn trường mầm non';
const pageDescription = 'Tư vấn cá nhân hóa 1-1 từ chuyên gia tâm lý trẻ em. Khám phá 5 sai lầm phổ biến, cách giúp con thích nghi nhanh, và bí quyết chọn trường phù hợp.';
const canonicalUrl = 'https://truongvietanh.com/tuyen-sinh/mam-non/tu-van';
---

<!doctype html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{pageTitle}</title>
  <meta name="description" content={pageDescription} />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href={canonicalUrl} />
  <meta property="og:title" content={pageTitle} />
  <meta property="og:description" content={pageDescription} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonicalUrl} />
  <meta name="twitter:card" content="summary_large_image" />

  <style is:inline>:root{
      --n: #26275D;          /* navy deep */
      --b: #26275D;          /* brand navy */
      --o: #f9dd0e;          /* gold accent (replaces pink) */
      --od: #f9dd0e;         /* gold darker */
      --w: #fff;
      --g1: #F5F7FA;
      --g2: #DFE3E8;
      --g6: #5F6B7A;
      --g8: #1C2A3A;
      --gn: #1AAB6E;
      --f: 'Open Sans', system-ui, -apple-system, 'Segoe UI', sans-serif;
    }*{ margin: 0; padding: 0; box-sizing: border-box; }body{
      font-family: var(--f);
      -webkit-font-smoothing: antialiased;
      font-size: 16px;
      color: var(--w);
      background: linear-gradient(135deg, #26275D 0%, #26275D 40%, #162040 100%);
      min-height: 100vh;
      min-height: 100dvh;
    }/* Logo */
    .logo-bar{
      text-align: center;
      padding: 20px 12px 0;
    }.logo-bar img{
      height: 64px;
      opacity: .9;
      loading: lazy;
    }/* Layout */
    .wrap{
      display: flex;
      max-width: 1100px;
      margin: 0 auto;
      padding: 20px 16px 32px;
      gap: 44px;
      align-items: center;
      min-height: calc(100vh - 84px);
      min-height: calc(100dvh - 84px);
    }.L{ flex: 1; }.R{ width: 480px; flex-shrink: 0; }/* Badge */
    .badge{
      display: inline-flex;
      align-items: center;
      gap: 5px;
      background: rgba(249, 221, 14,.18);
      border: 1px solid rgba(249, 221, 14,.42);
      color: var(--o);
      padding: 6px 16px;
      border-radius: 16px;
      font-size: 16px;
      font-weight: 700;
      margin-bottom: 16px;
    }.badge .dot{
      width: 6px;
      height: 6px;
      background: var(--o);
      border-radius: 50%;
      animation: p 1.5s infinite;
    }
    @keyframes p {0%, 100%{ opacity: 1; }50%{ opacity: .3; }
    }/* Heading */
    h1{
      font-family: 'Montserrat', var(--f);
      font-size: clamp(28px, 4.5vw, 38px);
      font-weight: 800;
      line-height: 1.18;
      margin-bottom: 20px;
    }h1 em{
      color: var(--o);
      font-style: normal;
    }/* Perks list */
    .perks{
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 12px;
      margin-bottom: 24px;
    }.perks li{
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 16px;
      color: rgba(255,255,255,.85);
    }.perks .ic{
      width: 32px;
      height: 32px;
      background: rgba(255,255,255,.08);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      flex-shrink: 0;
    }/* Social proof */
    .proof{
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      background: rgba(255,255,255,.06);
      border-radius: 10px;
      border: 1px solid rgba(255,255,255,.08);
      margin-bottom: 16px;
    }.proof b{
      font-size: 26px;
      color: var(--o);
      line-height: 1;
    }.proof span{
      font-size: 16px;
      color: rgba(255,255,255,.55);
      line-height: 1.3;
    }/* Footer contact */
    .foot{
      font-size: 16px;
      color: rgba(255,255,255,.5);
      text-align: center;
    }.foot a{
      color: var(--o);
      text-decoration: none;
      font-weight: 600;
    }/* FORM CARD */
    .F{
      background: var(--w);
      border-radius: 20px;
      padding: 32px 28px;
      box-shadow: 0 20px 60px rgba(0,0,0,.4);
      color: var(--g8);
    }.F h2{
      font-family: 'Montserrat', var(--f);
      font-size: 24px;
      font-weight: 800;
      color: var(--b);
      text-align: center;
      line-height: 1.25;
      margin-bottom: 4px;
    }.F .fd{
      font-size: 16px;
      color: #475569;
      text-align: center;
      margin-bottom: 18px;
      font-weight: 500;
    }.F .fd b{ color: var(--o); }/* Step dots */
    .sd{
      display: flex;
      justify-content: center;
      gap: 5px;
      margin-bottom: 14px;
    }.sd i{
      display: block;
      height: 4px;
      border-radius: 2px;
      background: var(--g2);
      width: 30px;
      transition: .3s;
    }.sd i.on{ background: var(--b); width: 48px; }.sd i.ok{ background: var(--gn); }/* Form fields */
    .fg{ margin-bottom: 14px; }.fg label{
      display: block;
      font-size: 16px;
      font-weight: 600;
      color: var(--g8);
      margin-bottom: 4px;
    }.fg input{
      width: 100%;
      padding: 14px 14px;
      border: 2px solid var(--g2);
      border-radius: 10px;
      font-size: 16px;
      font-family: var(--f);
      background: var(--g1);
      transition: .15s;
    }.fg input:focus{
      outline: 0;
      border-color: var(--b);
      background: var(--w);
      box-shadow: 0 0 0 3px rgba(38,39,93,.15);
    }/* Buttons */
    .btn{
      width: 100%;
      padding: 18px 16px;
      border: none;
      border-radius: 11px;
      font-size: 18px;
      font-weight: 800;
      cursor: pointer;
      font-family: var(--f);
      transition: all .2s;
      min-height: 56px;
    }.bo{
      background: var(--o);
      color: var(--b);
      box-shadow: 0 4px 16px rgba(249, 221, 14,.35);
    }.bo:hover{
      background: var(--od);
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(249, 221, 14,.45);
    }.btn:disabled{
      opacity: .5;
      cursor: default;
      transform: none;
      box-shadow: none;
    }/* Form meta */
    .fm{
      text-align: center;
      font-size: 16px;
      color: var(--g6);
      margin-top: 10px;
    }.fm a{
      color: var(--b);
      font-weight: 600;
    }.fb{
      display: flex;
      justify-content: center;
      gap: 14px;
      margin-top: 8px;
      font-size: 16px;
      color: var(--g6);
    }/* Steps */
    .s2, .sok{ display: none; }.sok{
      text-align: center;
      padding: 16px 0;
    }.sok .ck{
      font-size: 40px;
      margin-bottom: 8px;
    }.sok h3{
      font-size: 24px;
      color: var(--n);
      font-weight: 800;
    }.sok p{
      font-size: 16px;
      color: var(--g6);
      margin-top: 6px;
      line-height: 1.5;
    }.sok a{
      color: var(--b);
      font-weight: 700;
      text-decoration: none;
      font-size: 17px;
    }

    /* Responsive */
    @media (max-width: 800px) {.wrap{
        flex-direction: column;
        padding: 16px 12px 24px;
        gap: 24px;
        min-height: auto;
      }.L{
        text-align: center;
        align-items: center;
        display: flex;
        flex-direction: column;
      }h1{ font-size: 26px; }.perks{
        align-items: flex-start;
        max-width: 340px;
      }.proof{ justify-content: center; }.R{ width: 100%; max-width: 380px; }.F{ padding: 26px 20px; }
    }

    /* Google Fonts */
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Open+Sans:wght@400;600;700&display=swap&subset=vietnamese');
  </style>

  <!-- GTM dataLayer -->
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    window.dataLayer.push({
      'event': 'page_view',
      'page_title': 'Tư Vấn Cá Nhân Hóa Lộ Trình - Mầm Non',
      'page_url': 'https://truongvietanh.com/tuyen-sinh/mam-non/tu-van',
      'school_level': 'mam-non',
      'funnel_code': 'pain-point-mn'
    });
  </script>
<!-- Google Tag Manager -->
<script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','GTM-KPL2NSR9');</script>
<!-- End Google Tag Manager -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "EducationalOrganization",
    "name": "Trường Mầm Non Việt Anh",
    "url": "https://truongvietanh.com/tuyen-sinh/mam-non/tu-van",
    "description": "Tư vấn cá nhân hóa 1-1 từ chuyên gia tâm lý trẻ em. Khám phá 5 sai lầm phổ biến, cách giúp con thích nghi nhanh, và bí quyết chọn trường phù hợp.",
    "telephone": "+84-916-961-409",
    "address": {"@type": "PostalAddress", "addressLocality": "TP. Hồ Chí Minh", "addressCountry": "VN"},
    "parentOrganization": {"@type": "EducationalOrganization", "name": "Trường Liên Cấp Việt Anh", "url": "https://truongvietanh.com"}
  }
  </script>
</head>
<body>
<!-- Google Tag Manager (noscript) -->
<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-KPL2NSR9"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<!-- End Google Tag Manager (noscript) -->

  <div class="logo-bar">
    <img src="/logo-vietanh-yellow.png" alt="Trường Việt Anh" loading="lazy" />
  </div>

  <div class="wrap">
    <div class="L">
      <div class="badge">
        <span class="dot"></span>
        💡 Tư vấn cá nhân hóa 1-1
      </div>
      <h1>
        Con Hay Khóc Khi Đi Học?<br/>
        <em>5 Sai Lầm Thường Gặp</em><br/>
        Khi Chọn Trường Mầm Non
      </h1>
      <ul class="perks">
        <li><div class="ic">✓</div> 5 sai lầm phổ biến khi chọn trường (99% phụ huynh mắc phải)</li>
        <li><div class="ic">✓</div> Cách giúp con thích nghi nhanh, không khóc, yêu trường</li>
        <li><div class="ic">✓</div> Dấu hiệu cảnh báo: trường không phù hợp với con em</li>
        <li><div class="ic">✓</div> Bí quyết từ chuyên gia tâm lý trẻ em 15+ năm kinh nghiệm</li>
        <li><div class="ic">✓</div> Nhận tư vấn cá nhân hóa 1-1 (không ràng buộc)</li>
      </ul>
      <img src="https://media.truongvietanh.com/images/phu-huynh-04.webp" alt="Tư vấn tuyển sinh mầm non" loading="lazy" width="1200" height="auto" style="border-radius:12px;width:100%;height:auto;margin:1.5rem 0" />
      <div class="proof">
        <b>247</b>
        <span>phụ huynh đã được tư vấn</span>
      </div>
      <div class="foot">
        📞 <a href="tel:0916961409">0916 961 409</a> · truongvietanh.com
      </div>
    </div>

    <div class="R">
      <div class="F">
        <!-- Step 1: Email -->
        <div class="s1" id="s1">
          <div class="sd"><i class="on"></i><i></i></div>
          <h2>Nhận Tư Vấn Cá Nhân Hóa Lộ Trình</h2>
          <div class="fd">Chuyên gia tâm lý trẻ em sẽ tư vấn <b>1-1 riêng cho con bạn</b></div>
          <form id="f1">
            <div class="fg">
              <label>Email của ba/mẹ</label>
              <input type="email" id="email" placeholder="email@example.com" autocomplete="email" required />
            </div>
            <button type="submit" class="btn bo" id="b1">Nhận tư vấn ngay →</button>
          </form>
          <div class="fm">🔒 Bảo mật · Tư vấn cá nhân hóa Lộ trình học tập</div>
          <div class="fb">
            <span>✓ Miễn phí</span>
            <span>✓ Không ràng buộc</span>
          </div>
        </div>

        <!-- Step 2: Name + Phone -->
        <div class="s2" id="s2">
          <div class="sd"><i class="ok"></i><i class="on"></i></div>
          <h2>Thêm 1 Bước → Nhận Tư Vấn!</h2>
          <div class="fd"><b>247 phụ huynh</b> đã nhận tư vấn chuyên sâu — Chuyên gia sẽ gọi <b>trong 2 giờ</b></div>
          <form id="f2">
            <div class="fg">
              <label>Họ tên ba/mẹ</label>
              <input type="text" id="nm" placeholder="Nguyễn Văn A" required />
            </div>
            <div class="fg">
              <label>Số điện thoại (Zalo)</label>
              <input type="tel" id="ph" placeholder="0912 345 678" required />
            </div>
            <button type="submit" class="btn bo" id="b2">Xác nhận tư vấn →</button>
          </form>
          <div class="fm"><a href="#" id="sk">Bỏ qua →</a></div>
        </div>

        <!-- Success -->
        <div class="sok" id="sok">
          <div class="ck">🎉</div>
          <h3>Đăng ký thành công!</h3>
          <p>Chuyên gia tâm lý sẽ gọi trong 2 giờ<br/>📞 <a href="tel:0916961409">0916 961 409</a></p>
        </div>
      </div>
    </div>
  </div>

  <script is:inline>
    const WEBHOOK = '/api/lead';
    const $ = s => document.getElementById(s);

    $('f1').onsubmit = async e => {
      e.preventDefault();
      const b = $('b1'), v = $('email').value.trim();
      if (!v) return;
      b.disabled = 1;
      b.textContent = 'Đang gửi...';
      try {
        await fetch(WEBHOOK, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            step: 'partial_capture',
            email: v,
            source: 'pain_point_mn',
            page: window.location.href,
            utm_source: new URLSearchParams(location.search).get('utm_source') || '',
            utm_medium: new URLSearchParams(location.search).get('utm_medium') || '',
            utm_campaign: new URLSearchParams(location.search).get('utm_campaign') || '',
            timestamp: new Date().toISOString()
          })
        });
      } catch (err) {
        console.error('[LeadForm] Step 1 error:', err);
      }
      $('s1').style.display = 'none';
      $('s2').style.display = 'block';
      window.dataLayer?.push({ event: 'lead_s1', variant: 'pain_point', email: v });
    };

    $('f2').onsubmit = async e => {
      e.preventDefault();
      $('b2').disabled = 1;
      try {
        await fetch(WEBHOOK, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            step: 'full_submit',
            email: $('email').value.trim(),
            fullName: $('nm').value.trim(),
            phone: $('ph').value.trim(),
            source: 'pain_point_mn',
            page: window.location.href,
            timestamp: new Date().toISOString()
          })
        });
      } catch (err) {
        console.error('[LeadForm] Step 2 error:', err);
      }
      ok();
      window.dataLayer?.push({ event: 'lead_done', variant: 'pain_point' });
    };

    $('sk').onclick = e => { e.preventDefault(); ok(); };

    function ok() {
      const name = $('nm')?.value?.trim() || '';
      const email = $('email')?.value?.trim() || '';
      window.location.href = '/cam-on?name=' + encodeURIComponent(name) + '&email=' + encodeURIComponent(email);
    }
  </script>
</body>
</html>
